# 프로젝트3 설치 및 운영 가이드 (간단 버전)

> **프로젝트명**: MediaPipe 기반 맨몸운동 자동 카운터 시스템  
> **목표**: 카메라 영상만으로 사람의 반복 동작을 분석해 운동 횟수를 자동 측정

---

## 📋 빠른 시작

### 어떤 버전을 설치해야 하나?

| 상황 | 설치할 버전 | 시간 |
|------|------------|------|
| **실제 사용 (운동 카운팅)** | 모바일 버전만 | 10분 |
| **인수인계 받는 입장** | 모바일 버전 (최우선) | 10분 |
| **알고리즘 개발/수정** | PC 버전 + 모바일 버전 | 20분 |

**👉 인수인계 받는 입장이라면 → 모바일 버전만 설치하세요!**

### python 파일 개요

**HEADPOSE 프로젝트 (얼굴 방향/감정 추정):**
- 얼굴 방향 추정: `headpose_webcam_direction.py` (좌/우/정면 판별, 추천), `head_pose.py` (기본), `head_pose_standard.py` (캘리브레이션), `recognizeTest_ld4.py/ld6.py/ld10.py` (랜드마크 개수별)
- 감정 추정: `emotion_mediapipe.py` (단일 인원), `emotion_mediapipe_multi.py` (다인원)
- Zone 인식: `face_body_pose.py` (화면 4분할, Zone 1~4), `zone.py` (매트 ROI + Zone)

**PC 버전 (운동 카운팅):**
- 기본 테스트: `test_pose_webcam.py`, `test_camera.py`
- 운동 카운팅 (v0.4): `main_squat.py`, `main_pushup.py`, `main_crunch.py` 등

---

## 모바일 버전 설치 (권장)

> **⭐ 중요**: 모바일 버전이 실제 사용자가 사용하는 최종 결과물입니다.

### 1. Flutter 설치 확인

```bash
flutter doctor
```

**확인 사항:**
- `Flutter` 항목에 `[✓]` 표시
- `Android toolchain` 항목에 `[✓]` 표시
- `Android Studio` 항목에 `[✓]` 표시

**문제 발생 시:**
- Flutter 미설치: https://flutter.dev/docs/get-started/install
- Android Studio 미설치: https://developer.android.com/studio

### 2. 저장소 클론

```bash
git clone https://github.com/pnkmem432/workout-counter-mobile.git
cd workout-counter-mobile
```

### 3. 브랜치 선택

```bash
# 브랜치 목록 확인
git branch -a

# extras 브랜치로 전환 (모든 기능 포함, 권장)
git checkout extras

# 또는 특정 운동만 테스트
git checkout squat  # 스쿼트만
```

### 4. 의존성 설치

```bash
flutter pub get
```

### 5. 실행

```bash
# 연결된 기기 확인
flutter devices

# 앱 실행
flutter run
```

**확인 사항:**
- [ ] 앱이 기기에서 실행되는가?
- [ ] 로그인/회원가입 화면이 나타나는가?
- [ ] 운동 카운팅이 작동하는가?

---

## PC 버전 설치 (알고리즘 개발용)

> **⚠️ 주의**: PC 버전은 알고리즘 검증용 프로토타입입니다.  
> 실제 사용을 위해서는 모바일 버전을 설치하세요.

### 1. Python 설치 확인

```bash
python --version
```

**⚠️ 중요**: Python 3.10.8을 권장합니다.  
Python 3.13+에서는 MediaPipe 0.10.21이 작동하지 않을 수 있습니다.

**Python 3.10.8 설치:**
- 다운로드: https://www.python.org/downloads/release/python-3108/
- Windows: 설치 시 "Add Python 3.10 to PATH" 체크 (선택사항)

### 2. 저장소 클론

```bash
git clone https://github.com/pnkmem432/workout-counter-pc.git
cd workout-counter-pc
```

### 3. 가상환경 생성 및 활성화

```bash
# 가상환경 생성 (Python 3.10.8 사용)
py -3.10 -m venv .venv  # Windows
# 또는
python3.10 -m venv .venv  # macOS/Linux

# 가상환경 활성화
.\.venv\Scripts\activate  # Windows PowerShell
.venv\Scripts\activate.bat  # Windows CMD
source .venv/bin/activate  # macOS/Linux
```

**활성화 확인:** 프롬프트 앞에 `(.venv)` 표시

### 4. 라이브러리 설치

```bash
pip install mediapipe==0.10.21
pip install opencv-python==4.11.0.86
pip install numpy
```

### 5. 실행

```bash
# 기본 테스트 (MediaPipe Pose 확인)
python test_pose_webcam.py

# 또는 v0.4 버전 실행 (운동 카운팅)
git checkout v0.4
cd v0.4
python main_squat.py
```

**종료:** ESC 키 또는 `q` 키

---

## 주요 트러블슈팅

### Python 관련

| 문제 | 해결 방법 |
|------|----------|
| `ModuleNotFoundError: No module named 'mediapipe'` | 가상환경 활성화 확인 (`(.venv)` 표시) |
| `AttributeError: module 'mediapipe' has no attribute 'solutions'` | Python 3.10.8 사용 + MediaPipe 0.10.21 설치 |
| `ERROR: Could not find a version that satisfies the requirement mediapipe==0.10.21` | Python 3.10.8 설치 필수 |

### 가상환경 관련

**Windows PowerShell 보안 오류:**
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

또는 CMD 사용:
```cmd
.venv\Scripts\activate.bat
```

### 실행 관련

| 문제 | 해결 방법 |
|------|----------|
| 웹캠이 인식되지 않음 | 다른 프로그램 종료, 카메라 번호 변경 (`cv2.VideoCapture(0)` → `cv2.VideoCapture(1)`) |
| 포즈 인식이 안 됨 | 조명 개선, 카메라 위치 조정 (정면, 전신이 보이도록) |

---

## 운영 환경 설정

### 권장 촬영 환경

| 운동 | 권장 높이 | 비고 |
|------|----------|------|
| **스쿼트** | 90cm (배~가슴 높이) | CALIB 빠름, 포즈 인식 안정 |
| **푸쉬업** | 65cm (무릎~골반 높이) | 카운트 안정적 |
| **크런치** | 65cm (무릎~골반 높이) | 포즈 유지 필요 |
| **버피테스트** | 65cm (무릎~골반 높이) | Light/Dark 모두 안정적 |

**⚠️ 중요:**
- ❌ **바닥 위치는 부적합**: 포즈 인식 실패, CALIB 어려움
- ✅ **삼각대 고정 필수**: 손으로 들고 있으면 미세한 떨림으로 READY 실패
- ✅ **밝은 환경 권장**: Light 환경이 Dark 환경보다 항상 안정적

---

## 저장소

- **PC 버전**: https://github.com/pnkmem432/workout-counter-pc
- **모바일 버전**: https://github.com/pnkmem432/workout-counter-mobile
- **HEADPOSE** (별도 실험 프로젝트): https://github.com/pnkmem432/HEADPOSE

---

## 참고

- **상세 가이드**: `프로젝트3_설치_운영_가이드.md` 참고
- **Python 3.10.8 설치 가이드**: `Python_3.10.8_설치_가이드.md` 참고

---

**작성일**: 2026-01-28  
**버전**: 간단 버전
