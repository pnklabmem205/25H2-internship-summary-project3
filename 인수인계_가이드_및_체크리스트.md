# 인수인계 가이드 및 체크리스트

> **목적**: IoT 파트를 제외한 ML/소프트웨어를 1차 인수인계하고, 나중에 IoT를 겸직으로 인수인계할 수 있도록 **전체를 정리해두고** 파트별로 분리·검수할 수 있는 구조를 만든다.  
> **인수인계자(정재진)**: 선행 지식 없이 따라하기 → 구동 확인 → AI 도구로 이해할 수 있는 형태까지 단계별로 수렴.

---

## 1. 진행자(인수인계 주는 쪽)에게 정리해달라고 요청할 항목

아래는 **인수인계 받는 쪽이 요청할 때 쓸 수 있는 체크리스트형 문구**이다.  
진행자가 “뭘 해야 하지?”에서 빠지지 않도록, **해야 할 일·산출물·형식**을 명시했다.

---

### 1.1 저장소·백업

| # | 요청 항목 | 산출물 / 확인 방법 | 비고 |
|---|-----------|-------------------|------|
| 1.1.1 | GitHub 저장소가 최신 상태로 푸시되어 있는지 | `git status` / `git log` clean, 원격과 동기화 | 릴리스 태그 있으면 명시 |
| 1.1.2 | 프로젝트별 저장소 URL 목록 | `README.md` 또는 인수인계 문서에 정리 | PC/모바일/서버/펌웨어 구분 |
| 1.1.3 | (선택) GitHub Actions / CI 설정 | `.github/` 폴더, 빌드·테스트 스크립트 | 있으면 문서화 |

---

### 1.2 실행 환경·의존성 (정재진이 다운로드 후 실행 가능하게)

| # | 요청 항목 | 산출물 / 확인 방법 | 비고 |
|---|-----------|-------------------|------|
| 1.2.1 | **Python** | 버전 명시 (예: 3.10, 3.11) | `pyproject.toml` / `runtime.txt` 등 |
| 1.2.2 | **requirements.txt** (또는 `Pipfile`, `poetry.lock`) | 패키지명 + 버전 고정 | `pip freeze` 또는 lock 파일 |
| 1.2.3 | **Node/npm** (Flutter·Nest 등 있을 경우) | `package.json` + `package-lock.json` / `pubspec.lock` | 버전 명시 |
| 1.2.4 | **.env.example** | 실제 비밀 제외, 키 이름·설명·예시값 | `DATABASE_URL`, `API_KEY` 등 |
| 1.2.5 | **.env** | 로컬 전용이면 제외, 공유 불가 확인 | 인수인계 시 별도 전달(비밀관리) |
| 1.2.6 | **DB** (MySQL/PostgreSQL 등) | 스키마 DDL 또는 마이그레이션 + **스냅샷(백업) 파일** | 복원 절차 1줄 요약 |
| 1.2.7 | **IoT 펌웨어** (ESP-IDF, Arduino 등) | 툴체인 버전, 보드 타입, 빌드 명령 | `idf.py --version` 등 |

---

### 1.3 문서 이관 (Notion → GitHub 등)

| # | 요청 항목 | 산출물 / 확인 방법 | 비고 |
|---|-----------|-------------------|------|
| 1.3.1 | **최종보고서** | Notion → `docs/최종보고서.md` 또는 `README.md` 섹션 | 마크다운, 이미지 경로 정리 |
| 1.3.2 | **중간보고서** | `docs/중간보고서/` 또는 구글 드라이브 동기화 목록 | 링크를 README에 명시 |
| 1.3.3 | **기획안, 실험 결과, 참고 자료** | `docs/기획안.md`, `docs/실험결과/`, `docs/참고자료.md` | 회원가입·외부 서비스 링크 포함 |
| 1.3.4 | **플로우차트** | Mermaid 또는 PNG/SVG, `docs/` 또는 루트 | `README.md`에서 링크 |

---

### 1.4 데이터·실험 산출물

| # | 요청 항목 | 산출물 / 확인 방법 | 비고 |
|---|-----------|-------------------|------|
| 1.4.1 | **실험 데이터, 엑셀 등** | Google Drive 폴더 구조 + `docs/실험데이터_목록.md` | DB 이관 여부는 1.4.2에서 결정 |
| 1.4.2 | **실험 데이터 DB 이관** | (선택) 이관 스크립트 또는 CSV/Excel → DB 임포트 절차 | 인수인계자와 협의 후 “한다/안 한다” 결정 |

---

### 1.5 코드·AI 활용

| # | 요청 항목 | 산출물 / 확인 방법 | 비고 |
|---|-----------|-------------------|------|
| 1.5.1 | **주석·문서화** | AI로 주석 보강한 뒤, PR 또는 인수인계 브랜치에 반영 | 함수·모듈·핵심 분기 위주 |
| 1.5.2 | **프로젝트 개요 MD** | `README.md` 또는 `docs/프로젝트_개요.md`: 목표, 구조, 실행 절차, 트러블슈팅 | AI·신규 인력이 파악하기 좋은 형태 |

---

### 1.6 IoT 전용 (겸직 인수인계용 — 나중에 사용)

| # | 요청 항목 | 담당(예) | 산출물 / 확인 방법 | 비고 |
|---|-----------|----------|-------------------|------|
| 1.6.1 | **잘 되는 케이스·권장 환경** | 김건우 등 | `docs/IoT_권장_가이드.md`: 펌웨어/하드웨어/조명/위치 | “이렇게 하면 된다” |
| 1.6.2 | **잘 안 되는 케이스·금지/주의** | 김건우 등 | `docs/IoT_주의_가이드.md`: 실패하는 환경, 하면 안 되는 설정 | “이러면 안 된다” |
| 1.6.3 | **특정 실패 케이스 기록** | 김건우 등 | 이슈 템플릿 또는 `docs/IoT_트러블슈팅.md` | 증상, 환경, 조치 |

---

### 1.7 검수·크로스체크

| # | 요청 항목 | 담당 | 산출물 / 확인 방법 | 비고 |
|---|-----------|------|-------------------|------|
| 1.7.1 | **코드 분석·AI 주석** | 조연호, 정재진 | AI 분석 결과 + 사람이 크로스체크한 요약 | `docs/코드분석_검수.md` 등 |

---

## 2. 인수인계·체크리스트를 “어떻게” 받는 것이 합리적인가

### 2.1 받는 형식

- **체크리스트**  
  - 1장짜리 **체크리스트(엑셀/시트/Notion DB)**  
  - 각 항목: `[ ]` / `[x]`, 비고, 담당, 기한  
  - 인수인계자(정재진)가 직접 체크하면서 진행

- **문서 위치**  
  - **Notion** 또는 **Google 시트** 1개를 “인수인계 마스터 체크리스트”로 지정  
  - 링크를 `인수인계_가이드_및_체크리스트.md`(본 문서)에 넣어두면, 나중에 AI·다른 팀원도 접근 가능

### 2.2 수신 절차 (합리적 순서)

1. **진행자**가 §1 항목대로 자료를 채운다.  
2. **진행자**가 “인수인계 패키지”를 한 번에 전달 (ZIP 또는 공유 폴더 정리).  
3. **인수인계자(정재진)**가 §3의 “인수인계자 체크리스트” 순서로 **따라하기** → 구동 확인.  
4. **검수자(조연호)**가 §3의 “검수자 체크리스트”로 최종 점검.  
5. **진행자**는 “진행자 체크리스트”에 자기 완료만 표시; 인수인계자·검수자가 보완 요청 시 보완.

### 2.3 “따라하기”가 되도록 할 것

- **Runbook(실행 절차서)**  
  - “1) 저장소 clone → 2) 가상환경 생성 → 3) `pip install -r requirements.txt` → 4) `.env` 설정 → 5) `python main.py` 또는 `flutter run`”처럼 **번호 붙은 단계**로 정리.  
  - 진행자가 **한 번 정재진 PC(또는 동일 스펙 VM)에서 그대로 따라해보고**, 끊기는 단계를 문서에 반영.

- **실패 시 기록**  
  - “이 버전에서는 안 됨”, “이 순서로 하면 됨” 등을 `docs/트러블슈팅.md` 또는 Runbook 하단에 적어두면, AI·다음 인수인계에도 유리.

---

## 3. 진행자 / 인수인계자 / 검수자 — 역할별 체크리스트

### 3.1 역할 정의

| 역할 | 담당 | 책임 |
|------|------|------|
| **진행자** | 인수인계 주는 사람 (기존 개발자) | 자료 정리, Runbook·체크리스트 작성, 인수인계 시연 및 Q&A |
| **인수인계자** | 정재진 | 따라하기, 구동 확인, 체크리스트 기입, 불명확한 점 정리하여 진행자·검수자에게 전달 |
| **검수자** | 조연호 | 진행자·인수인계자 산출물 검토, 체크리스트 최종 확인, 누락·위험 지점 지적 |

---

### 3.2 진행자 체크리스트 (인수인계 주는 쪽이 할 일)

**저장소·백업**

- [ ] 1.1.1 GitHub 최신 푸시, `git status` clean
- [ ] 1.1.2 프로젝트별 저장소 URL을 README/인수인계 문서에 정리
- [ ] 1.1.3 (해당 시) CI 설정 존재 시 문서화

**실행 환경·의존성**

- [ ] 1.2.1 Python 버전 명시
- [ ] 1.2.2 `requirements.txt`(또는 동일 역할 파일) 버전 고정
- [ ] 1.2.3 (해당 시) Node/npm, Flutter `pubspec.lock` 버전 명시
- [ ] 1.2.4 `.env.example` 작성 (키 이름·설명·예시값)
- [ ] 1.2.5 `.env`는 비밀 유지, 인수인계 시 별도 안전 전달
- [ ] 1.2.6 DB 스키마·마이그레이션 + 스냅샷(백업) 및 복원 절차
- [ ] 1.2.7 (해당 시) IoT 툴체인·보드·빌드 명령 문서화

**문서 이관**

- [ ] 1.3.1 최종보고서 Notion → `docs/` 또는 `README.md`
- [ ] 1.3.2 중간보고서 위치·링크 정리
- [ ] 1.3.3 기획안, 실험 결과, 참고 자료, 회원가입 등 `docs/` 또는 링크
- [ ] 1.3.4 플로우차트 가독성 확보, `README`에서 링크

**데이터·실험**

- [ ] 1.4.1 실험 데이터·엑셀 등 Google Drive 목록 및 `docs/실험데이터_목록.md`
- [ ] 1.4.2 (협의 후) 실험 데이터 DB 이관 절차 여부

**코드·AI**

- [ ] 1.5.1 AI 등으로 핵심 코드 주석 보강, PR/브랜치 반영
- [ ] 1.5.2 `README` 또는 `docs/프로젝트_개요.md` — AI·신규 인력 이해용

**IoT(겸직 대비)**

- [ ] 1.6.1 (해당 시) `docs/IoT_권장_가이드.md`
- [ ] 1.6.2 (해당 시) `docs/IoT_주의_가이드.md`
- [ ] 1.6.3 (해당 시) `docs/IoT_트러블슈팅.md` 또는 이슈 템플릿

**Runbook**

- [ ] Runbook(실행 절차서) 작성: clone → 설치 → 설정 → 실행
- [ ] 진행자 본인 PC(또는 동일 스펙)에서 Runbook 그대로 수행해 끊긴 단계 수정

---

### 3.3 인수인계자 체크리스트 (정재진 — 따라하기·구동 확인)

**사전**

- [ ] 저장소 clone, `docs/`·`README` 훑어보기
- [ ] Runbook·체크리스트 받기 (Notion/시트 링크 확인)

**1차: 환경 구축**

- [ ] Python(및 필요한 런타임) 설치, 버전 일치
- [ ] `requirements.txt` / `pubspec.lock` 등으로 의존성 설치
- [ ] `.env` 설정 (`.env.example` 참고, 비밀은 진행자에게 별도 수령)
- [ ] (해당 시) DB 스냅샷 복원 후 접속 확인

**2차: 따라하기·구동**

- [ ] Runbook 1단계부터 순서대로 수행
- [ ] **실제 구동** (예: 서버 기동, 앱 빌드·실행, 최소 1회 데모 시나리오)
- [ ] 막힌 단계 있으면 `docs/트러블슈팅.md`에 기록 또는 진행자에게 전달

**3차: 문서·AI 활용**

- [ ] `README`·`docs/프로젝트_개요.md` 읽고, AI(Cursor 등)에 “이 프로젝트 구조 요약해줘” 질의 → 이해 여부 자가 점검
- [ ] (가능 시) 코드 일부를 AI에게 설명 요청해, 인수인계자 본인이 이해할 수 있는 수준인지 확인

**4차: 검수 요청**

- [ ] 인수인계자 체크리스트 self-check 완료
- [ ] 검수자(조연호)에게 체크리스트·Runbook·트러블슈팅 링크 전달

---

### 3.4 검수자 체크리스트 (조연호)

**저장소·패키지**

- [ ] GitHub 최신 반영, 브랜치·태그 정리
- [ ] `requirements.txt`·`pubspec.lock` 등 의존성 고정 여부
- [ ] `.env.example` 존재, 비밀 불포함

**문서**

- [ ] 최종·중간보고서, 기획안, 실험 정리가 `docs/` 또는 링크로 연결
- [ ] 플로우차트 확인 가능
- [ ] `README` 또는 `docs/프로젝트_개요.md`가 AI·신규 인력 파악에 적절한지

**실행·Runbook**

- [ ] Runbook이 “따라하기”에 충분한지 (버전·명령·순서)
- [ ] (가능 시) 검수자 환경에서 1회 Runbook 수행 또는 인수인계자 데모 확인

**코드·AI**

- [ ] AI 주석·`docs/코드분석_검수.md` 등 크로스체크 결과 확인
- [ ] “데모 세팅 요청 시 바로 구동 가능”에 가까운지 평가

**데이터·Google Drive**

- [ ] (조연호→진행자) Google Drive 실험 데이터·문서 목록 체크리스트 수령
- [ ] 인수인계 과정에서 해당 목록 직접 점검

**IoT(겸직 대비)**

- [ ] (해당 시) `IoT_권장_가이드`, `IoT_주의_가이드` 존재·가독성
- [ ] (해당 시) 김건우 등과 IoT 체크리스트·트러블슈팅 정합성 확인

**Must 요건 (연구개발 인수인계)**

- [ ] AI agent로 코드 인수인계·이해 가능 형태
- [ ] 프로젝트별 구동 재현 가능, 필요 시 데모 세팅 가능
- [ ] AI 주석 + `README`/`docs` 구성으로 GitHub에서 빠르게 이해 가능
- [ ] 조연호·정재진 크로스체크 완료

---

## 4. IoT vs 비IoT 분리 — 겸직 인수인계를 위한 구조

### 4.1 분리 원칙

- **1차 인수인계**: ML·백엔드·프론트·인프라(소프트웨어 전반) — 정재진이 체크리스트를 주로 수행.
- **2차(겸직) 인수인계**: IoT 펌웨어·하드웨어·측정/출입 등 **장비 구동·환경** — 동일 체크리스트를 쓰되, “IoT 전용” 항목만 추가로 수행.

### 4.2 문서·폴더 구조 예시 (프로젝트별)

```
프로젝트 루트/
├── README.md                 # 전체 개요, 링크 정리 (AI·인수인계 우선)
├── docs/
│   ├── 프로젝트_개요.md
│   ├── Runbook_소프트웨어.md   # 비IoT: 서버, 앱, 스크립트
│   ├── Runbook_IoT.md         # IoT: 펌웨어 빌드·플래시·장비 연결 (겸직용)
│   ├── 최종보고서.md
│   ├── IoT_권장_가이드.md     # (해당 시) 김건우 등
│   ├── IoT_주의_가이드.md
│   └── IoT_트러블슈팅.md
├── .env.example
└── ...
```

- `Runbook_소프트웨어.md`: 정재진 1차 인수인계 때 사용.  
- `Runbook_IoT.md` + `IoT_권장/주의/트러블슈팅`: 나중에 IoT 겸직 시 사용.  
- `인수인계_가이드_및_체크리스트.md`(본 문서)의 §3 체크리스트에 “(IoT)” 항목만 플래그를 달아두면, 1차에는 스킵, 2차에 only IoT로 돌릴 수 있다.

### 4.3 체크리스트에서의 플래그

- 진행자/인수인계자/검수자 체크리스트에  
  - `[ ] 1.6.x (IoT) …`  
  처럼 **(IoT)** 표시를 두고,  
  - 1차: (IoT) 제외하고 진행  
  - 2차: (IoT)만 모아서 진행  

- Notion/시트에서는 “파트” 컬럼에 `소프트웨어` / `IoT` 넣어서 필터로 나누면 된다.

---

## 5. AI 도구가 이해하기 좋은 형태 (요약)

- **파일 형식**: Markdown(`.md`) 우선.  
- **위치**: `README.md`, `docs/프로젝트_개요.md`, `docs/Runbook_*.md` 등 — 프로젝트 루트 인근.  
- **내용**:  
  - 목표, 디렉터리 구조, 실행 절차(번호 목록), 주요 모듈 1문장 요약.  
  - 플로우는 Mermaid 또는 다이어그램 이미지 + 짧은 설명.  
- **코드**:  
  - 모듈·클래스·핵심 함수에 1~2문장 주석.  
  - `docs/코드분석_검수.md`에 “어디가 핵심인지” 요약.  

이렇게 해두면 Cursor·ChatGPT 등에 “이 프로젝트 구조/이 함수 역할”을 물었을 때, 컨텍스트로 넣기 좋다.

---

## 6. 문서 인수인계 (Notion, Google Drive) — Must

- **Notion**: 최종보고서, 중간보고서 → `docs/`로 이관 or 링크 고정.  
- **Google Drive**:  
  - 조연호가 “실험 데이터·중간 보고·기타 문서” **목록 체크리스트**를 진행자에게 받고,  
  - 인수인계 시 그 목록을 **직접** 확인하며 체크.  
  - 인수인계자(정재진)는 해당 목록을 보면서 “필요한 것만” 추가로 받기.

---

## 7. 정리: 진행자에게 보낼 “한 줄 요청문” 예시

> “인수인계를 위해, `인수인계_가이드_및_체크리스트.md` §1(정리해달라고 요청할 항목)과 §3.2(진행자 체크리스트)를 기준으로 자료를 채워주시고, **Runbook(1) clone → (2) 설치 → (3) 설정 → (4) 실행** 순서로 정재진이 따라할 수 있는 실행 절차서를 만들어 주세요.  
> 일단 **소프트웨어(비IoT)** 위주로 하고, **IoT 전용**은 §1.6·§3.2의 (IoT) 항목으로 따로 정리해두시면, 나중에 겸직으로 인수인계할 때 사용하겠습니다.”

---

## 8. Change Report (본 가이드 적용 시)

| 항목 | 내용 |
|------|------|
| **무엇을** | 인수인계 시 진행자·인수인계자·검수자 역할, 요청 항목, 체크리스트, IoT/비IoT 분리, AI 활용 형태를 한 문서로 정리. |
| **왜** | “뭘 정리해달라”를 진행자에게 명확히 요청하고, 정재진은 따라하기→구동 확인→AI 이해를 한 번에 할 수 있게 하기 위함. |
| **이점** | 요청·수신·검수가 한 흐름으로 맞고, 겸직으로 IoT를 나중에 넣기 쉬움. AI·다음 인수인계에도 재사용 가능. |
| **운영 전환 시** | 데모· PoC뿐 아니라 상용화 시 “온보딩 문서”, “Runbook”으로 확장하기 좋은 기반. |
| **되돌리기** | Yes. 본 md만 제거/수정하면 기존 project1~3 info.md 체계는 유지 가능. |

---

## 부록: 마스터 체크리스트 (Notion DB / Google 시트 컬럼 예시)

엑셀·Notion DB·Google 시트에 넣어 **한 장에서** 관리할 때 쓰는 컬럼 구조 예시.

| 파트 | 역할 | # | 체크항목 | 완료 | 비고 |
|------|------|---|----------|:----:|------|
| 공통 | 진행자 | P1 | GitHub 최신 푸시, URL 목록 | | |
| 공통 | 진행자 | P2 | requirements.txt, .env.example, DB 스냅샷 | | |
| 공통 | 진행자 | P3 | 최종·중간보고서, 기획안, 플로우차트 이관 | | |
| 공통 | 진행자 | P4 | Runbook 작성 및 self-테스트 | | |
| 공통 | 진행자 | P5 | AI 주석, README/docs 프로젝트_개요 | | |
| IoT | 진행자 | P6 | IoT_권장_가이드, IoT_주의_가이드, 트러블슈팅 | | |
| 공통 | 인수인계자 | R1 | clone, 의존성, .env, DB 복원 | | |
| 공통 | 인수인계자 | R2 | Runbook 따라하기, 구동 확인 | | |
| 공통 | 인수인계자 | R3 | README·개요 문서 + AI로 이해 확인 | | |
| 공통 | 검수자 | V1 | 저장소, 의존성, .env.example 검토 | | |
| 공통 | 검수자 | V2 | 문서 이관, Runbook, 데모 가능 여부 | | |
| 공통 | 검수자 | V3 | AI 주석·코드분석 크로스체크, Google Drive 목록 | | |
| IoT | 검수자 | V4 | IoT 가이드·트러블슈팅 정합성 | | |

- **파트**: `공통` = 1차(비IoT) 인수인계, `IoT` = 겸직 2차 인수인계 때 추가.
- **완료**: `○` / `×` / `−`(해당 없음) 등 팀 규칙에 맞게.
- **비고**: 담당자, 기한, “진행자에게 재요청” 등.
