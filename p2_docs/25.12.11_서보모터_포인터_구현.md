---

- 출입 인증 시스템 + 서보모터 구성의 장단점
    - 장점
        1. 카메라가 사용자를 향해 움직여서 얼굴이 화면 중앙에 들어옴
        → 이론상 얼굴 인식 성공률이 올라갈 수 있음
    - 단점
        1. 출입 인증은 로직 자체가 문 앞에 잠깐 서서 인식하는 구조이기 때문에 카메라가 움직이지 않아도 됨
        2. 서보가 움직일 때의 소음이나 하루 수 백번 움직이면 내구성 문제가 생길 수 있음
        3. 사람이 둘 이상 있을 때 혼돈 발생

### 일반적인 PC + OpenCV + 서보모터 얼굴 트래킹

1. **얼굴 검출 (Detection)**
- 얼굴이 화면 어디에 있는지 처음 찾는 역할
    - OpenCV DNN or Haar Cascade 등 활용
    - 여기서 나온 faces(가장 큰 얼굴 검출 박스)를 트래커 초기화용 ROI로 넘김
- 검출된 얼굴 사각형의 중심 좌표를 트래킹 포인트로 사용
1. **추적 (Tracking)**
- 검출은 무겁고 프레임마다 돌리면 느리거나 튀기 때문에 검출로 한 번 잡고 그 다음엔 tracker로 계속 끌고가는 패턴 사용
    - OpenCV에서 흔히 쓰는 tracker:
        - KCF: 빠르고 꽤 안정적, 가장 많이 사용
        - CSRT: 더 정확하지만 좀 느림
        - MOSSE: 매우 빠른 대신 정확도나 내구성이 좀 떨어짐
    - 새로 얼굴이 검출되지 않아도 tracker는 ‘이전 프레임에서 여기 있었으니 이번 프레임에서는 이 방향으로 조금 움직였을 것’ 라고 예측하는 식으로 박스를 계속 갱신함
- 즉, 전용 trakcer가 있어서 검출 없이도 프레임 사이 위치를 예측하고 연속성 있게 모터가 돌아감

### ESP-WHO 의 face detection 통해 제어

- OpenCV처럼 ‘검출 후 추적’ 구조가 아니라 매 프레임마다 새로운 얼굴 검출을 반복하는 방식
1. **얼굴 검출 (Detection)**
- ESP-WHO는 매 프레임 실시간 얼굴 검출 수행
- OpenCV처럼 tracker를 사용하지 않기 때문에 얼굴이 사라지거나 흔들리면 위치 예측이 불가능하고 서보의 위치가 꼬이는 등의 문제가 다수 발생할 수 있음
- 서보는 느리고 검출은 빨라 속도 매칭이 안됨
    - 서보모터는  60도 움직이는데 약 0.1초 ~ 0.2초 → 기계적 움직임 속도 제한 있음
    - ESP-WHO는 15~25fps 로 얼굴 박스를 갱신 → 0.04초 ~ 0.06초마다 새로운 좌표가 들어옴
    - 서보는 느리게 움직이는데 얼굴 좌표는 너무 빨리 바뀌기 때문에 따라가다가 계속 엇나감
        - OpenCV에서는 tracker 때문에 좌표 변화가 아주 부드럽고 예측된 값
        - ESP-WHO 에서는 매 프레임 새 검출이라 뒤죽박죽
1. **히스테리시스 (Hysteresis)**
- 최근 검출된 얼굴 박스를 N 프레임 저장해두고 1~3 프레임 정도 미검출 상태가 발생해도 이전 박스를 사용해 지속적으로 제어 (짧은 시간용 tracking-like 동작)
- ESP-WHO 는 tracker가 없어서 박스가 순간적으로 사라지면 서보가 멈추거나 튀는 문제가 발생하기 때문에 이를 보완하기 위해 히스테리시스 로직을 구현함
- 하지만, 히스테리시스는 ‘박스 튐 보정’ 용 이지 트래킹이 아니기 때문에 연속적인 트래킹 동작을 구현할 수 없음
- 또한 ESP-WHO 얼굴 박스 자체가 OpenCV 보다 훨씬 불안정하여 박스 중심좌표가 항상 흔들림

- 사용자 출입 인증이 완료되어 사람이 입장을 할 때 그거에 맞춰 서보모터가 돌아가지 않음

→ 인증 후 움직이면 얼굴 검출이 되지 않기 때문에

→ 사람이 입장하는 방향 즉, 사람을 따라가는 구조가 아닌 사용자 인증 완료가 되면 ‘왼쪽/오른쪽으로 모터를 90도 돌리기’ 식 으로 사용자가 입장을 한다 를 표현할 수는 있음

## v1.2 실험 동작

https://github.com/pnkmem432/esp-who-face_entry/tree/v1.2/examples/human_face_recognition/main 

### 1. 목적

- ESP-WHO 얼굴 검출 결과를 이용해서 얼굴이 화면 어디에 있는지를 서보모터 방향으로 보여주는 포인터 구현
- 서보모터가 얼굴 검출을 따라올 수 있는지를 실험
    - 웹 UI 스트리밍 화면에서 얼굴 박스를 왼쪽/오른쪽/위/아래로 움직일 때 
    서보 `PAN/TILT`가 각각 **15 ↔ 60 ↔ 105** 방향으로 움직이는지 확인

### 2. 현재 구조

- 입력
    - ESP-WHO 카메라 프레임 (`cam_fb_t`)
    - 매 프레임마다 얼굴 검출 (`HumanFaceDetect`)
- 출력
    - JPEG 스트리밍용 오버레이 (얼굴 박스 + 라벨)
    - 얼굴 개수/인식 결과 기반 **인증 상태 머신** (LED)
    - 얼굴 위치 기반 **서보 PTZ 제어** (`servo_ptz_set(pan, tilt)`)

### 3. 얼굴 검출 & 히스테리시스

- 매프레임으로 얼굴 리스트 검출
- 히스테리시스 버퍼
    - 최근 검출 박스를 s_last_boxes에 저장
    - 최대 3개의 프레임까지 미검출이 나와도 이전 박스를 그대로 사용
    
    → 박스가 한두 프레임 사라져도 서보가 바로 멈추거나 튀지 않도록
    
    → 단, tracking 수준이 아니라 짧은 시간 버티기 수준
    

### 4. 서보 포인터 모드 로직

https://github.com/pnkmem432/esp-who-face_entry/blob/v1.2/examples/human_face_recognition/main/face_processor.cpp

- 기준 각도 세팅
    - 중앙 기준 각도
        - `SERVO_PAN_CENTER_DEG = 60°`
        - `SERVO_TILT_CENTER_DEG = 60°`
    - 범위
        - `SERVO_*_RANGE_DEG = 45°`
        - 결과적으로 각도 범위는
            - **15°(마이너스)** ↔ 60°(센터) ↔ **105°(플러스)**
- 얼굴 위치 → 화면 좌표
    - 히스테리시스로 결정된 얼굴들 중 **가장 큰 박스 1개 선택**
    - 그 박스의 중심 좌표 (cx, cy) 계산 후 화면 중앙 좌표 (center_x, center_y)와 비교해서
        - `nx = (cx - center_x) / center_x` (좌우 오차, -1 ~ +1)
        - `ny = (cy - center_y) / center_y` (상하 오차, -1 ~ +1)
- 데드존 & 민감도
    - 작은 흔들림은 무시
        
        ```
        // 데드존 (좌우/상하 별도)
        static const float SERVO_PAN_DEADZONE  = 0.02f;
        static const float SERVO_TILT_DEADZONE = 0.02f;
        ```
        
        - `|nx| < SERVO_PAN_DEADZONE` → 0으로 처리
        - `|ny| < SERVO_TILT_DEADZONE` → 0으로 처리
- 각도 매핑
    
    현재 코드에서 **각도 매핑은 이렇게 동작**:
    
    - `target_pan = 60° + nx * 45°`
    - `target_tilt = 60° + ny * 45°`
    - 화면 기준 동작:
        
        
        | 화면에서 얼굴 위치 | nx/ny 부호 | PAN/TILT 각도 변화 | 기구 기준 의미 |
        | --- | --- | --- | --- |
        | 왼쪽으로 이동 | nx < 0 | PAN ↓ → 60→15 쪽 | 서보가 왼쪽(마이너스)으로 |
        | 오른쪽으로 이동 | nx > 0 | PAN ↑ → 60→105 쪽 | 서보가 오른쪽(플러스)으로 |
        | 위쪽으로 이동 | ny < 0 | TILT ↓ → 60→15 쪽 | 서보가 위쪽(마이너스)으로 |
        | 아래쪽으로 이동 | ny > 0 | TILT ↑ → 60→105 쪽 | 서보가 아래(플러스)로 |
    - **각도 제한 →** `PAN: 10°~120°`, `TILT: 10°~120°`로 클램핑
    - **업데이트 주기**
        - `SERVO_UPDATE_INTERVAL_MS = 100ms`
        - 최소 100ms마다 한 번씩만 `servo_ptz_set()` 호출

### 5. 동작 영상

[KakaoTalk_20251211_174336588.mp4](attachment:e379ca6f-f171-4076-9c81-c0ca262b1d53:KakaoTalk_20251211_174336588.mp4)

- 스트리밍 되는 것보다 서보모터가 더 빠르게 움직이는거 같아 보이나 출입 인증 에서는 얼굴 검출이 되면서, 큰 움직임이 일어나는 상황이 거의 없다보니 서보모터 자체도 큰 각도로의 움직임은 보이지 않는 거 같음