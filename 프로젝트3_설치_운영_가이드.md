# 프로젝트3 설치 및 운영 가이드

> **프로젝트명**: MediaPipe 기반 맨몸운동 자동 카운터 시스템  
> **목표**: 카메라 영상만으로 사람의 반복 동작을 분석해 운동 횟수를 자동 측정

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [빠른 시작 - 어떤 버전을 설치해야 하나?](#2-빠른-시작---어떤-버전을-설치해야-하나)
3. [순서대로 따라하기 가이드](#순서대로-따라하기-가이드) ⭐ **권장**
4. [모바일 버전 설치 및 실행](#모바일-버전-설치-및-실행) ⭐ **최우선**
5. [PC 버전 설치 및 실행](#pc-버전-설치-및-실행) (선택)
6. [HEADPOSE 설치 및 실행](#headpose-설치-및-실행) (선택)
7. [운영 환경 설정](#5-운영-환경-설정)
8. [트러블슈팅](#6-트러블슈팅) ⚠️ **문제 발생 시 먼저 확인**
9. [macOS/Linux 호환성](#macoslinux-호환성-참고)

## 🔧 Python 3.10.8 설치 가이드

> **⚠️ 중요**: 프로젝트3는 Python 3.10.8을 권장합니다.  
> Python 3.13+에서는 MediaPipe 0.10.21이 작동하지 않을 수 있습니다.

**상세 설치 가이드**: [`Python_3.10.8_설치_가이드.md`](./Python_3.10.8_설치_가이드.md) 파일을 참고하세요.

**빠른 설치 (Windows):**
1. Python 3.10.8 다운로드: https://www.python.org/downloads/release/python-3108/
2. 설치 시 "Add Python 3.10 to PATH" 체크 (선택사항)
3. 가상환경 생성: `py -3.10 -m venv .venv`

---

## 1. 프로젝트 개요

### 1.1 프로젝트3: 운동 카운팅 시스템

프로젝트3는 **두 개의 레포지토리**로 구성되어 있습니다:

- **workout-counter-pc**: 알고리즘 검증용 (Python)
- **workout-counter-mobile**: 실제 사용용 앱 (Flutter) ⭐ **이것만 설치하면 됨**

**개발 순서**: PC 버전에서 알고리즘 검증 → 모바일 버전으로 이식

**저장소:**
- PC 버전: https://github.com/pnkmem432/workout-counter-pc
- 모바일 버전: https://github.com/pnkmem432/workout-counter-mobile

### 1.2 HEADPOSE: 별도 실험 프로젝트 (참고용)

> **⚠️ 참고**: HEADPOSE는 프로젝트3와는 **별개의 실험 프로젝트**입니다.  
> 같은 기간에 진행되었지만, 운동 카운팅과는 무관한 얼굴 방향/감정 추정 실험입니다.

**실제 개발 순서 (인턴 진행 순서):**
- **2025.12.15**: workout-counter-pc 시작 (프로젝트 기본 세팅)
- **2025.12.22**: workout-counter-pc v0.3 (정면 기준 운동 카운트)
- **2026.01.13~14**: HEADPOSE 시작 (얼굴 방향 추정 PoC)
- **2026.01.15**: HEADPOSE + workout-counter-mobile APK (병행 진행)
- **2026.01.22**: HEADPOSE 계속 (YOLO Pose + FaceMesh)
- **2026.01.26**: HEADPOSE 계속 (감정 추정)

→ **인턴은 workout-counter-pc를 먼저 시작하고, 나중에 HEADPOSE를 시작했습니다.**

**HEADPOSE 프로젝트 내용:**
- 얼굴 방향(Head Pose) 추정: FaceMesh + solvePnP 기반 yaw/pitch/roll 계산
- 감정 추정: 얼굴 랜드마크 기반 규칙 기반 감정 분류 (neutral/happy/sad/angry/surprised)
- Zone 인식: YOLO Pose + FaceMesh 기반 인원·영역 인식

**저장소:**
- HEADPOSE: https://github.com/pnkmem432/HEADPOSE

#### HEADPOSE와 workout-counter-pc의 공통점

**기술 스택 겹침:**
- 둘 다 Python 3.10.8 사용
- 둘 다 MediaPipe 사용 (HEADPOSE는 FaceMesh, workout-counter-pc는 Pose)
- 둘 다 OpenCV 사용 (비슷한 버전: 4.8.x ~ 4.11.x)
- 둘 다 NumPy 사용
- 둘 다 웹캠 기반 실시간 처리 (`cv2.VideoCapture(0)`)

**코드 패턴 겹침:**
- 둘 다 BGR → RGB 변환 필수 (`cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)`)
- 둘 다 MediaPipe 처리 후 결과 시각화
- 둘 다 OpenCV로 웹캠 읽기 및 화면 출력
- 둘 다 Python venv 가상환경 사용

**개발 환경 겹침:**
- 같은 개발 기간 (2025년 12월~2026년 1월)
- 같은 개발자가 작업
- 비슷한 라이브러리 버전 (MediaPipe 0.10.x, OpenCV 4.x)

**인수인계 시 참고:**

#### 설치 순서 추천

**옵션 1: workout-counter-pc 먼저 (인턴 순서, 권장)**
- ✅ 프로젝트3의 핵심이므로 우선순위가 높음
- ✅ 인턴이 실제로 진행한 순서와 동일
- ✅ 프로젝트3 인수인계에 집중 가능
- ⚠️ MediaPipe Pose가 처음이라면 학습 곡선이 있을 수 있음

**옵션 2: HEADPOSE 먼저 (학습용)**
- ✅ 더 간단한 예제로 MediaPipe/OpenCV 기본기 익힐 수 있음
- ✅ FaceMesh는 Pose보다 단순할 수 있어 초보자에게 접근성 높음
- ✅ 기술 스택이 동일하므로 workout-counter-pc로 쉽게 전환 가능
- ⚠️ 프로젝트3와 직접 관련 없어서 시간 투자 대비 효율 낮을 수 있음

**권장 순서:**
1. **프로젝트3 인수인계가 목적이라면**: workout-counter-pc 먼저
2. **MediaPipe/OpenCV가 처음이라면**: HEADPOSE 먼저 (선택사항)
3. **둘 다 설치할 계획이라면**: workout-counter-pc → HEADPOSE 순서

**참고:**
- HEADPOSE는 프로젝트3 인수인계와 직접 관련 없음
- 하지만 **개발 환경 설정은 거의 동일**하므로, HEADPOSE를 설치해봤다면 workout-counter-pc도 쉽게 설치 가능
- p3_docs에 HEADPOSE 관련 문서가 포함되어 있으나, 프로젝트3와는 별개
- HEADPOSE 관련 인수인계가 필요하면 별도로 진행

---

## 2. 빠른 시작 - 어떤 버전을 설치해야 하나?

프로젝트 구조를 이해하셨다면, 이제 어떤 버전을 설치할지 결정하세요.

### 설치 버전 선택 가이드

| 상황 | 설치할 버전 | 시간 | 저장소 |
|------|------------|------|--------|
| **실제 사용 (운동 카운팅)** | 모바일 버전만 | 10분 | [workout-counter-mobile](https://github.com/pnkmem432/workout-counter-mobile) |
| **인수인계 받는 입장** | 모바일 버전 (최우선) | 10분 | [workout-counter-mobile](https://github.com/pnkmem432/workout-counter-mobile) |
| **알고리즘 개발/수정** | PC 버전 + 모바일 버전 | 20분 | [workout-counter-pc](https://github.com/pnkmem432/workout-counter-pc) + [workout-counter-mobile](https://github.com/pnkmem432/workout-counter-mobile) |

**👉 인수인계 받는 입장이라면 → [모바일 버전 설치](#모바일-버전-설치-및-실행)로 바로 이동하세요!**

---

## 순서대로 따라하기 가이드

> **⭐ 권장**: 인턴이 실제로 진행한 순서대로 따라하면서 MediaPipe와 OpenCV를 단계적으로 학습할 수 있습니다.

### 전체 순서 개요

```
1. workout-counter-pc 설치 및 실행
   ↓ (MediaPipe Pose 기본기 습득)
2. HEADPOSE 기본 설치 및 실행
   ↓ (MediaPipe FaceMesh 기본기 습득)
3. HEADPOSE 고급 기능 (선택)
```

### 예상 소요 시간

| 단계 | 내용 | 시간 |
|------|------|------|
| 1단계 | workout-counter-pc 설치 및 실행 | 15분 |
| 2단계 | HEADPOSE 기본 설치 및 실행 | 10분 |
| 3단계 | HEADPOSE 고급 기능 (선택) | 20분 |
| **총계** | | **45분** |

---

### 1단계: workout-counter-pc 설치 및 실행

**목적**: MediaPipe Pose를 사용한 운동 카운팅 시스템 설치 및 실행

**진행 순서:**
1. [PC 버전 설치 및 실행](#pc-버전-설치-및-실행) 섹션의 1~5단계를 따라하세요
   - 1단계: Python 설치 확인
   - 2단계: 저장소 클론 (`git clone https://github.com/pnkmem432/workout-counter-pc.git`)
   - 3단계: 가상환경 생성 및 활성화
   - 4단계: 라이브러리 설치 (MediaPipe, OpenCV, NumPy)
   - 5단계: 실행 및 확인
     ```bash
     # 기본 테스트 (MediaPipe Pose 확인)
     python test_pose_webcam.py
     
     # 또는 v0.4 버전 실행 (운동 카운팅)
     git checkout v0.4  # v0.4 브랜치로 전환
     cd v0.4
     python main_squat.py
     ```

**실행 확인:**
- ⚠️ **가상환경 활성화 필수**: 실행 전에 `.\.venv\Scripts\activate` 실행
- 웹캠 창이 열리고 스켈레톤이 표시되는지 확인
- 스쿼트 동작을 하면 카운트가 올라가는지 확인 (v0.4 버전인 경우)
- ESC 키를 누르면 종료되는지 확인

**확인 사항:**
- [ ] 웹캠이 정상적으로 열리는가?
- [ ] 스켈레톤이 화면에 표시되는가?
- [ ] 운동을 하면 카운트가 올라가는가?
- [ ] READY → CALI → COUNT 상태 전환이 정상인가?

**문제 발생 시:**
- 웹캠이 인식되지 않으면: [PC 버전 설치 및 실행](#pc-버전-설치-및-실행) 섹션의 "5단계: 실행 및 확인" → "문제 발생 시" 참고
- 라이브러리 오류가 발생하면: 가상환경이 활성화되었는지 확인 (`(.venv)` 표시 확인)

**완료 후:**
- ✅ MediaPipe Pose 기본 사용법을 익혔습니다
- ✅ OpenCV로 웹캠을 다루는 방법을 배웠습니다
- ✅ BGR → RGB 변환이 왜 필요한지 이해했습니다
- ✅ MediaPipe Pose의 33개 랜드마크 구조를 이해했습니다

**다음 단계:** → [2단계: HEADPOSE 기본 설치](#2단계-headpose-기본-설치-및-실행)로 이동

---

### 2단계: HEADPOSE 기본 설치 및 실행

**목적**: MediaPipe FaceMesh를 사용한 얼굴 방향 추정 시스템 설치 및 실행

**진행 순서:**
1. [HEADPOSE 설치 및 실행](#headpose-설치-및-실행) 섹션의 1~5단계를 따라하세요
2. 기본 얼굴 방향 추정이 정상 동작하는지 확인

**확인 사항:**
- [ ] 웹캠이 정상적으로 열리는가?
- [ ] 얼굴이 인식되는가?
- [ ] yaw/pitch/roll 값이 표시되는가?

**완료 후:**
- MediaPipe FaceMesh 기본 사용법을 익혔습니다
- solvePnP를 사용한 3D 각도 계산을 배웠습니다
- workout-counter-pc와 HEADPOSE의 공통점을 이해했습니다

**다음 단계:** → [3단계: HEADPOSE 고급 기능](#3단계-headpose-고급-기능-선택)으로 이동 (선택)

---

### 3단계: HEADPOSE 고급 기능 (선택)

**목적**: HEADPOSE의 추가 기능들을 탐색

**선택 가능한 기능:**
- 감정 추정 (emotion_mediapipe.py)
- 다인원 얼굴 인식 (emotion_mediapipe_multi.py)
- Zone 인식 (face_body_pose.py, zone.py)

**진행 방법:**
- [HEADPOSE 설치 및 실행](#headpose-설치-및-실행) 섹션의 "추가 기능 실행" 부분을 참고하세요

**완료 후:**
- MediaPipe의 다양한 활용 방법을 이해했습니다
- 프로젝트3와 HEADPOSE의 차이점을 명확히 알게 되었습니다

---

## 모바일 버전 설치 및 실행

> **⭐ 중요**: 모바일 버전이 실제 사용자가 사용하는 최종 결과물입니다.  
> 인수인계 받는 입장에서는 이 섹션만 따라하면 됩니다.

### 📌 전체 설치 체크리스트

- [ ] 1단계: Flutter 설치 확인
- [ ] 2단계: 저장소 클론
- [ ] 3단계: 브랜치 선택
- [ ] 4단계: 의존성 설치
- [ ] 5단계: 실행 및 확인

---

### 1단계: Flutter 설치 확인

**목적**: Flutter가 제대로 설치되어 있는지 확인합니다.

#### 실행할 명령어

```bash
flutter doctor
```

#### 예상 결과

**Windows 예시:**
```
Doctor summary (to see all details, run flutter doctor -v):
[✓] Flutter (Channel stable, 3.x.x)
[✓] Android toolchain - develop for Android devices
[✓] Chrome - develop for the web
[✓] Visual Studio - develop Windows apps
[✓] Android Studio
[✓] VS Code
[✓] Connected device
[✓] Network resources
```

**macOS 예시:**
```
Doctor summary (to see all details, run flutter doctor -v):
[✓] Flutter (Channel stable, 3.x.x)
[✓] Android toolchain - develop for Android devices
[✓] Chrome - develop for the web
[✓] Xcode - develop for iOS and macOS
[✓] Android Studio
[✓] VS Code
[✓] Connected device
[✓] Network resources
```

**참고**: 운영체제에 따라 일부 항목이 다를 수 있습니다 (예: Windows는 Visual Studio, macOS는 Xcode)

#### 확인 사항

- [ ] `Flutter` 항목에 `[✓]` 표시가 있는가?
- [ ] `Android toolchain` 항목에 `[✓]` 표시가 있는가?
- [ ] `Android Studio` 항목에 `[✓]` 표시가 있는가?

#### 문제 발생 시

**문제**: `Flutter` 항목에 `[✗]` 표시
- **해결**: Flutter 설치 필요
  - 설치 가이드: https://flutter.dev/docs/get-started/install
  - Windows: https://docs.flutter.dev/get-started/install/windows
  - macOS: https://docs.flutter.dev/get-started/install/macos
  - Linux: https://docs.flutter.dev/get-started/install/linux

**문제**: `Android toolchain` 항목에 `[✗]` 표시
- **해결**: Android Studio 설치 및 Android SDK 설정 필요
  - Android Studio 다운로드: https://developer.android.com/studio
  - 설치 후 Android SDK 설정

**문제**: `Android Studio` 항목에 `[✗]` 표시
- **해결**: Android Studio 설치 필요 (위 링크 참고)

#### 다음 단계

모든 항목이 `[✓]`로 표시되면 → [2단계: 저장소 클론](#2단계-저장소-클론)으로 이동

---

### 2단계: 저장소 클론

**목적**: GitHub에서 프로젝트 코드를 내 PC로 가져옵니다.

#### 실행할 명령어

**터미널 위치**: 원하는 작업 폴더
- Windows 예시: `C:\WS\flutter_projects` 또는 `D:\projects`
- macOS/Linux 예시: `~/projects` 또는 `/Users/username/projects`

```bash
git clone https://github.com/pnkmem432/workout-counter-mobile.git
cd workout-counter-mobile
```

#### 예상 결과

```
Cloning into 'workout-counter-mobile'...
remote: Enumerating objects: XXX, done.
remote: Counting objects: 100% (XXX/XXX), done.
remote: Compressing objects: 100% (XXX/XXX), done.
remote: Total XXX (delta XXX), reused XXX (delta XXX), pack-reused XXX
Receiving objects: 100% (XXX/XXX), XXX KiB | XXX MiB/s, done.
Resolving deltas: 100% (XXX/XXX), done.
```

그리고 현재 폴더가 `workout-counter-mobile`로 변경됩니다.

#### 확인 사항

- [ ] `workout-counter-mobile` 폴더가 생성되었는가?
- [ ] `cd workout-counter-mobile` 실행 후 폴더 안으로 들어갔는가?
- [ ] `lib/` 폴더가 있는가? (프로젝트 구조 확인)

#### 문제 발생 시

**문제**: `git: command not found` 또는 `'git'은(는) 내부 또는 외부 명령...`
- **해결**: Git이 설치되지 않음
  - Git 다운로드: https://git-scm.com/download/win
  - 설치 후 터미널 재시작

**문제**: `fatal: could not read Username for 'https://github.com'`
- **해결**: 저장소가 비공개인 경우 인증 필요
  - GitHub 계정으로 로그인 또는 SSH 키 설정

**문제**: 네트워크 오류
- **해결**: 인터넷 연결 확인, 방화벽 설정 확인

#### 다음 단계

저장소 클론이 완료되면 → [3단계: 브랜치 선택](#3단계-브랜치-선택)으로 이동

---

### 3단계: 브랜치 선택

**목적**: 프로젝트는 운동별로 브랜치가 분리되어 있습니다. 원하는 브랜치를 선택합니다.

#### 사용 가능한 브랜치

| 브랜치 | 설명 | 추천 |
|--------|------|------|
| `squat` | 스쿼트 카운팅만 | 테스트용 |
| `pushup` | 푸쉬업 카운팅만 | 테스트용 |
| `crunch` | 크런치 카운팅만 | 테스트용 |
| `burpee` | 버피테스트 카운팅만 | 테스트용 |
| `extras` | **모든 기능 포함** (로그인, 기록 등) | ⭐ **권장** |

#### 실행할 명령어

**브랜치 목록 확인:**
```bash
git branch -a
```

**브랜치 전환 (extras 브랜치 권장):**
```bash
git checkout extras
```

또는 특정 운동만 테스트하고 싶다면:
```bash
git checkout squat
```

#### 예상 결과

```
Switched to branch 'extras'
Your branch is up to date with 'origin/extras'.
```

또는 브랜치가 없으면:
```
error: pathspec 'extras' did not match any file(s) known to git.
```

이 경우 원격 브랜치를 가져와야 합니다:
```bash
git fetch origin
git checkout extras
```

#### 확인 사항

- [ ] `git branch -a` 실행 시 브랜치 목록이 보이는가?
- [ ] `git checkout extras` 실행 후 `Switched to branch 'extras'` 메시지가 나오는가?
- [ ] 현재 브랜치 확인: `git branch` (현재 브랜치에 `*` 표시)

#### 문제 발생 시

**문제**: `error: pathspec 'extras' did not match`
- **해결**: 원격 브랜치를 먼저 가져오기
  ```bash
  git fetch origin
  git checkout extras
  ```

**문제**: 브랜치 목록이 보이지 않음
- **해결**: 저장소 클론이 제대로 되었는지 확인
  ```bash
  # macOS/Linux
  ls -la
  
  # Windows
  dir
  ```

#### 다음 단계

브랜치 선택이 완료되면 → [4단계: 의존성 설치](#4단계-의존성-설치)로 이동

---

### 4단계: 의존성 설치

**목적**: 프로젝트가 사용하는 라이브러리들을 다운로드합니다.

#### 실행할 명령어

```bash
flutter pub get
```

#### 예상 결과

```
Running "flutter pub get" in workout-counter-mobile...
Resolving dependencies...
Got dependencies!
```

또는 처음 실행 시:
```
Running "flutter pub get" in workout-counter-mobile...
Resolving dependencies...
Downloading packages...
Got dependencies!
```

#### 확인 사항

- [ ] `Got dependencies!` 메시지가 나오는가?
- [ ] `pubspec.lock` 파일이 생성되었는가?
- [ ] 오류 메시지가 없는가?

#### 문제 발생 시

**문제**: `Error: Unable to find git in your PATH`
- **해결**: Git이 PATH에 없음
  - Git 재설치 또는 PATH 환경변수에 Git 추가

**문제**: `Error: No pubspec.yaml file found`
- **해결**: 잘못된 폴더에 있음
  - `cd workout-counter-mobile` 실행하여 프로젝트 폴더로 이동

**문제**: 네트워크 오류 (패키지 다운로드 실패)
- **해결**: 
  - 인터넷 연결 확인
  - 방화벽/프록시 설정 확인
  - 다시 시도: `flutter pub get`

**문제**: 버전 충돌 오류
- **해결**: 
  ```bash
  flutter clean
  flutter pub get
  ```

#### 다음 단계

의존성 설치가 완료되면 → [5단계: 실행 및 확인](#5단계-실행-및-확인)으로 이동

---

### 5단계: 실행 및 확인

**목적**: 앱을 실행하여 정상 동작을 확인합니다.

#### 5-1. 연결된 기기 확인

**실행할 명령어:**
```bash
flutter devices
```

**예상 결과:**
```
2 connected devices:

sdk gphone64 arm64 (mobile) • emulator-5554 • android-arm64  • Android 13 (API 33) (emulator)
SM-G991N (mobile)           • R58M30XXXXX   • android-arm64  • Android 13 (API 33)
```

또는:
```
No devices found.
```

#### 확인 사항

- [ ] Android 기기 또는 에뮬레이터가 연결되어 있는가?
- [ ] 기기 이름이 표시되는가?

#### 문제 발생 시

**문제**: `No devices found`
- **해결 방법 1**: Android 기기 연결
  - USB 케이블로 Android 기기 연결
  - 기기에서 "USB 디버깅" 허용
  - `flutter devices` 다시 실행

- **해결 방법 2**: Android 에뮬레이터 실행
  - Android Studio 실행
  - AVD Manager에서 에뮬레이터 시작
  - `flutter devices` 다시 실행

**문제**: 기기가 인식되지 않음
- **해결**: 
  - USB 디버깅 모드 활성화 확인
  - USB 드라이버 설치 확인
  - USB 케이블 교체

#### 5-2. 앱 실행

**실행할 명령어:**
```bash
flutter run
```

**예상 결과:**
```
Launching lib/main.dart on SM-G991N in debug mode...
Running Gradle task 'assembleDebug'...
...
✓ Built build/app/outputs/flutter-apk/app-debug.apk (XX.XMB).
Installing build/app/outputs/flutter-apk/app-debug.apk...
Flutter run key commands.
r Hot reload.
R Hot restart.
h List all available interactive commands.
d Detach (terminate "flutter run" but leave application running).
c Clear the screen
q Quit (terminate the application on the device).
```

그리고 앱이 기기에서 실행됩니다.

#### 확인 사항

- [ ] 앱이 기기에서 실행되는가?
- [ ] 로그인/회원가입 화면이 나타나는가?
- [ ] 오류 없이 실행되는가?

#### 문제 발생 시

**문제**: `Gradle task failed`
- **해결**: 
  ```bash
  cd android
  ./gradlew clean
  cd ..
  flutter clean
  flutter pub get
  flutter run
  ```

**문제**: `Error: ADB not found`
- **해결**: Android SDK 경로 설정 확인
  - Android Studio에서 SDK 경로 확인
  - 환경변수 `ANDROID_HOME` 설정

**문제**: 앱이 실행되지만 바로 종료됨
- **해결**: 
  - `flutter run` 출력에서 오류 메시지 확인
  - 로그 확인: `flutter logs`

#### 5-3. 기본 기능 확인

앱이 실행되면 다음을 확인하세요:

- [ ] **로그인/회원가입**
  - 회원가입 버튼 클릭
  - 닉네임과 비밀번호(4자리 숫자) 입력
  - 회원가입 성공 확인

- [ ] **운동 카운팅**
  - 홈 화면에서 "운동 시작" 클릭
  - 운동 선택 (스쿼트/푸쉬업/크런치/버피)
  - 카운팅 화면 진입 확인
  - 스켈레톤 오버레이 표시 확인

- [ ] **기록 조회**
  - 홈 화면에서 "나의 기록" 클릭
  - 기록 화면 진입 확인

#### 다음 단계

모든 기능이 정상 동작하면 → [APK 빌드](#apk-빌드-선택-사항) 또는 [운영 환경 설정](#5-운영-환경-설정)으로 이동

---

### APK 빌드 (선택 사항)

**목적**: 앱을 APK 파일로 빌드하여 다른 기기에 설치할 수 있게 합니다.

#### 실행할 명령어

```bash
flutter clean
flutter pub get
flutter build apk --release
```

#### 예상 결과

```
Running "flutter pub get" in workout-counter-mobile...
Got dependencies!

Building APK...
...
✓ Built build/app/outputs/flutter-apk/app-release.apk (XX.XMB).
```

#### APK 파일 위치

```
workout-counter-mobile/
└─ build/
   └─ app/
      └─ outputs/
         └─ flutter-apk/
            └─ app-release.apk  ← 이 파일
```

#### APK 설치 방법

1. **APK 파일을 Android 기기로 전송**
   - USB 케이블로 복사
   - Google Drive에 업로드 후 다운로드
   - 이메일로 전송

2. **기기에서 설치 허용**
   - 설정 → 보안 → "알 수 없는 소스" 허용
   - 또는 APK 파일 실행 시 허용

3. **APK 파일 실행하여 설치**

#### 확인 사항

- [ ] `app-release.apk` 파일이 생성되었는가?
- [ ] 파일 크기가 0이 아닌가? (일반적으로 20MB 이상)
- [ ] APK를 기기에 설치할 수 있는가?

#### 문제 발생 시

**문제**: `Gradle build failed`
- **해결**: 
  ```bash
  cd android
  
  # macOS/Linux
  ./gradlew clean
  
  # Windows
  gradlew.bat clean
  
  cd ..
  flutter clean
  flutter pub get
  flutter build apk --release
  ```

**문제**: APK 파일이 생성되지 않음
- **해결**: 빌드 로그에서 오류 메시지 확인
  - `flutter build apk --release -v` (상세 로그)

---

## PC 버전 설치 및 실행

> **⚠️ 주의**: PC 버전은 알고리즘 검증용 프로토타입입니다.  
> 실제 사용을 위해서는 [모바일 버전](#모바일-버전-설치-및-실행)을 설치하세요.

### 📌 전체 설치 체크리스트

- [ ] 1단계: Python 설치 확인
- [ ] 2단계: 저장소 클론
- [ ] 3단계: 가상환경 생성
- [ ] 4단계: 라이브러리 설치
- [ ] 5단계: 실행 및 확인

---

### 1단계: Python 설치 확인

**목적**: Python이 제대로 설치되어 있는지 확인합니다.

#### 실행할 명령어

```bash
python --version
```

#### 예상 결과

```
Python 3.10.8
```

> **⚠️ 중요**: Python 3.10.8을 사용하는 것을 강력히 권장합니다.  
> Python 3.13+에서는 MediaPipe 0.10.21이 작동하지 않을 수 있습니다.

또는:
```
Python 3.11.x  # 작동할 수 있지만 3.10.8 권장
Python 3.13.x  # MediaPipe 0.10.21 작동 안 함
```

#### 확인 사항

- [ ] Python 버전이 3.10.8인가? (권장)
- [ ] Python 버전이 3.10 이상인가? (최소 요구사항)
- [ ] `python` 명령어가 인식되는가?

#### 문제 발생 시

**문제**: `'python'은(는) 내부 또는 외부 명령...` (Windows) 또는 `command not found: python` (macOS/Linux)
- **해결**: Python이 설치되지 않음
  - Python 다운로드: https://www.python.org/downloads/
  - Windows: 설치 시 "Add Python to PATH" 체크 필수
  - macOS: `python3` 명령어 사용 또는 Homebrew로 설치: `brew install python@3.10`
  - Linux: `sudo apt-get install python3.10` (Ubuntu/Debian) 또는 해당 배포판 패키지 매니저 사용
  - 설치 후 터미널 재시작

**문제**: Python 버전이 3.10 미만
- **해결**: Python 3.10 이상 설치 필요

**문제**: Python 3.13+ 버전이 설치되어 있고, Python 3.10.8로 다운그레이드하고 싶음
- **상세 가이드**: `Python_3.10.8_설치_가이드.md` 파일 참고
- **해결**: Python 3.10.8을 추가로 설치 (기존 Python과 함께 사용 가능)
  
  **Windows:**
  1. Python 3.10.8 다운로드: https://www.python.org/downloads/release/python-3108/
     - "Windows installer (64-bit)" 선택
  2. 설치 시:
     - ✅ "Add Python 3.10 to PATH" 체크 (선택사항, 다른 버전과 충돌 가능)
     - ✅ "Install for all users" 체크 (선택사항)
     - "Customize installation" 선택하여 설치 경로 확인
  3. 설치 확인:
     ```powershell
     # Python 3.10.8 확인
     py -3.10 --version
     # 또는
     python3.10 --version
     ```
  4. 가상환경 생성 시 Python 3.10.8 지정:
     ```powershell
     # 방법 1: py launcher 사용 (권장)
     py -3.10 -m venv .venv
     
     # 방법 2: 전체 경로 사용
     C:\Users\사용자명\AppData\Local\Programs\Python\Python310\python.exe -m venv .venv
     
     # 방법 3: python3.10 명령어 사용 (PATH에 등록된 경우)
     python3.10 -m venv .venv
     ```
  
  **macOS:**
  ```bash
  # Homebrew로 Python 3.10.8 설치
  brew install python@3.10
  
  # 가상환경 생성
  python3.10 -m venv .venv
  ```
  
  **Linux:**
  ```bash
  # Ubuntu/Debian
  sudo apt-get update
  sudo apt-get install python3.10 python3.10-venv
  
  # 가상환경 생성
  python3.10 -m venv .venv
  ```
  
  **기존 가상환경이 있다면:**
  ```powershell
  # 기존 가상환경 삭제
  Remove-Item -Recurse -Force .venv
  
  # Python 3.10.8로 새 가상환경 생성
  py -3.10 -m venv .venv
  
  # 가상환경 활성화
  .\.venv\Scripts\activate
  
  # 버전 확인
  python --version  # Python 3.10.8이어야 함
  ```

#### 다음 단계

Python이 정상 설치되면 → [2단계: 저장소 클론](#2단계-저장소-클론-1)으로 이동

---

### 2단계: 저장소 클론

**목적**: GitHub에서 PC 버전 프로젝트 코드를 가져옵니다.

#### 실행할 명령어

**터미널 위치**: 원하는 작업 폴더

```bash
git clone https://github.com/pnkmem432/workout-counter-pc.git
cd workout-counter-pc
```

#### 예상 결과

```
Cloning into 'workout-counter-pc'...
...
Resolving deltas: 100% (XXX/XXX), done.
```

#### 확인 사항

- [ ] `workout-counter-pc` 폴더가 생성되었는가?
- [ ] `test_pose_webcam.py` 파일이 있는가?
- [ ] `test_camera.py` 파일이 있는가?

> **참고**: main 브랜치에는 테스트 파일만 있습니다.  
> v0.4 버전의 운동별 카운팅 파일(`main_squat.py` 등)이 필요하면 `git checkout v0.4`로 브랜치를 전환하세요.

#### 다음 단계

저장소 클론이 완료되면 → [3단계: 가상환경 생성](#3단계-가상환경-생성)으로 이동

---

### 3단계: 가상환경 생성

**목적**: 프로젝트 전용 Python 환경을 만듭니다. (다른 프로젝트와 충돌 방지)

> **💡 팁**: HEADPOSE 프로젝트를 이미 설치해봤다면, 이 단계는 동일합니다.  
> HEADPOSE와 workout-counter-pc는 같은 Python 환경 설정을 사용합니다.

> **⚠️ 중요**: Python 3.10.8을 사용하는 것을 강력히 권장합니다.  
> Python 3.13+에서는 MediaPipe 0.10.21이 작동하지 않을 수 있습니다.

#### 실행할 명령어

**Python 3.10.8이 설치되어 있는 경우:**
```bash
# Windows (py launcher 사용)
py -3.10 -m venv .venv

# 또는 python3.10 명령어 사용 (PATH에 등록된 경우)
python3.10 -m venv .venv
```

**일반 Python 명령어 사용 (현재 Python 버전 사용):**
```bash
python -m venv .venv
```

> **참고**: `python -m venv`를 사용하면 현재 기본 Python 버전이 사용됩니다.  
> Python 3.13+가 기본이라면 위의 `py -3.10` 명령어를 사용하세요.

#### 예상 결과

명령어 실행 후 아무 메시지도 나오지 않으면 성공입니다.

#### 확인 사항

- [ ] `.venv` 폴더가 생성되었는가?
- [ ] `.venv/Scripts/` 폴더가 있는가?

#### 가상환경 활성화

**Windows PowerShell:**
```bash
.\.venv\Scripts\activate
```

**Windows CMD:**
```bash
.venv\Scripts\activate.bat
```

**macOS / Linux:**
```bash
source .venv/bin/activate
```

**활성화 성공 시:**
- Windows: `(.venv) PS C:\...\workout-counter-pc>`
- macOS/Linux: `(.venv) user@macbook workout-counter-pc %`

프롬프트 앞에 `(.venv)`가 표시되면 성공입니다.

#### 가상환경 비활성화

가상환경을 끄려면 (비활성화하려면):

```bash
deactivate
```

**비활성화 후:**
- 프롬프트에서 `(.venv)` 표시가 사라짐
- 일반 Python 환경으로 돌아감

**가상환경 재생성 시:**
```bash
# 1. 가상환경 비활성화 (활성화되어 있다면)
deactivate

# 2. 기존 가상환경 폴더 삭제
# Windows PowerShell
Remove-Item -Recurse -Force .venv

# Windows CMD
rmdir /s /q .venv

# macOS/Linux
rm -rf .venv

# 3. 새 가상환경 생성
py -3.10 -m venv .venv  # Windows (Python 3.10.8)
python3.10 -m venv .venv  # macOS/Linux
```

#### 문제 발생 시

**문제**: `python -m venv` 명령어 실패
- **해결**: Python이 제대로 설치되었는지 확인
  - `python --version` 실행
  - macOS: `python3 --version` (맥은 python3 사용)

**문제**: 가상환경 활성화 실패 (Windows) - 보안 오류 발생
- **증상**: 
  ```
  .\.venv\Scripts\activate : 이 시스템에서 스크립트를 실행할 수 없으므로...
  또는
  cannot be loaded because running scripts is disabled on this system
  ```
- **원인**: PowerShell 실행 정책(Execution Policy)이 스크립트 실행을 차단함
- **해결 방법**:
  
  **방법 1: 실행 정책 변경 (권장)**
  ```powershell
  # 현재 실행 정책 확인
  Get-ExecutionPolicy
  
  # 실행 정책 변경 (현재 사용자만)
  Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
  
  # 확인 메시지가 나오면 Y 입력
  ```
  
  **방법 2: CMD 사용 (임시 해결)**
  ```cmd
  # PowerShell 대신 CMD 사용
  .venv\Scripts\activate.bat
  ```
  
  **방법 3: 우회 방법 (일회성)**
  ```powershell
  # 실행 정책을 우회하여 활성화
  & .\.venv\Scripts\Activate.ps1
  ```
  
  **방법 4: 관리자 권한으로 실행 정책 변경 (시스템 전체)**
  ```powershell
  # PowerShell을 관리자 권한으로 실행 후
  Set-ExecutionPolicy RemoteSigned -Scope LocalMachine
  ```
  
  > **💡 참고**: `RemoteSigned`는 로컬에서 작성한 스크립트는 실행 가능하고, 인터넷에서 다운로드한 스크립트는 서명된 것만 실행 가능합니다. 안전한 설정입니다.

**문제**: 가상환경 활성화 실패 (macOS/Linux)
- **해결**: 
  - `source .venv/bin/activate` 명령어 확인
  - `.venv/bin/activate` 파일이 존재하는지 확인: `ls .venv/bin/activate`

#### 다음 단계

가상환경이 활성화되면 → [4단계: 라이브러리 설치](#4단계-라이브러리-설치)로 이동

---

### 4단계: 라이브러리 설치

**목적**: 프로젝트가 사용하는 Python 라이브러리들을 설치합니다.

> **💡 팁**: HEADPOSE 프로젝트를 이미 설치해봤다면, MediaPipe와 OpenCV는 이미 설치되어 있을 수 있습니다.  
> 하지만 버전이 다를 수 있으니 확인 후 설치하세요.

#### 실행할 명령어

**가상환경이 활성화된 상태에서:**
```bash
pip install mediapipe==0.10.21
pip install opencv-python==4.11.0.86
pip install numpy
```

> **⚠️ 버전 문제 발생 시**:  
> MediaPipe 0.10.21이 더 이상 사용 불가능할 수 있습니다 (Python 3.13+ 또는 최신 pip 환경).  
> 이 경우 최신 버전으로 설치하세요:
> ```bash
> pip install mediapipe
> pip install opencv-python
> pip install numpy
> ```
> 최신 버전도 대부분 호환되지만, 문제가 발생하면 Python 3.10.8 환경을 사용하세요.

**이미 HEADPOSE를 설치했다면:**
- MediaPipe는 이미 설치되어 있을 수 있음 (HEADPOSE는 0.10.9 또는 0.10.21 사용)
- OpenCV 버전 확인: `pip show opencv-python`
  - HEADPOSE는 4.8.1.78 또는 4.11.0.86 사용
  - workout-counter-pc는 4.11.0.86 권장
  - 버전이 다르면 위 명령어로 재설치

#### 예상 결과

```
Collecting mediapipe==0.10.21
  Downloading mediapipe-0.10.21-cp310-cp310-win_amd64.whl (XX.X MB)
...
Successfully installed mediapipe-0.10.21 opencv-python-4.11.0.86 numpy-1.26.4
```

#### 확인 사항

- [ ] `Successfully installed` 메시지가 나오는가?
- [ ] 설치된 패키지 확인: `pip list`
  - `mediapipe 0.10.21` 표시되는가?
  - `opencv-python 4.11.0.86` 표시되는가?

#### 문제 발생 시

**문제**: `pip` 명령어가 인식되지 않음
- **해결**: 가상환경이 활성화되었는지 확인
  - 프롬프트에 `(.venv)` 표시 확인

**문제**: 패키지 다운로드 실패
- **해결**: 
  - 인터넷 연결 확인
  - 다시 시도: `pip install mediapipe==0.10.21`

**문제**: 버전 충돌 또는 특정 버전을 찾을 수 없음
- **해결**: 
  ```bash
  pip install --upgrade pip
  # 특정 버전이 없으면 최신 버전 사용
  pip install mediapipe opencv-python numpy
  ```
  
**문제**: `ERROR: Could not find a version that satisfies the requirement mediapipe==0.10.21`
- **원인**: Python 3.13+ 환경에서 MediaPipe 0.10.21이 지원되지 않음
- **해결**: 
  - ⚠️ **Python 3.10.8 사용 필수**: 프로젝트는 MediaPipe 0.10.21을 기준으로 작성되었고, 이 버전은 Python 3.10.8에서만 안정적으로 작동합니다
  - Python 3.10.8 설치: https://www.python.org/downloads/release/python-3108/
  - 새 가상환경 생성: `python3.10 -m venv .venv` 또는 `py -3.10 -m venv .venv` (Windows)

**문제**: 최신 MediaPipe 설치 후 `AttributeError: module 'mediapipe' has no attribute 'solutions'` 발생
- **원인**: MediaPipe 0.10.30+ 버전에서는 `solutions` 모듈이 제거됨
- **해결**: 
  - MediaPipe 0.10.21로 다운그레이드: `pip uninstall mediapipe -y && pip install mediapipe==0.10.21`
  - 또는 Python 3.10.8 환경 사용 (권장)

#### 다음 단계

라이브러리 설치가 완료되면 → [5단계: 실행 및 확인](#5단계-실행-및-확인-1)으로 이동

---

### 5단계: 실행 및 확인

**목적**: PC 버전이 정상 동작하는지 확인합니다.

> **⚠️ 중요**: 실행하기 전에 **가상환경이 활성화되어 있어야 합니다**.  
> 프롬프트 앞에 `(.venv)`가 표시되어 있는지 확인하세요.  
> 활성화되지 않았다면: `.\.venv\Scripts\activate` (Windows) 또는 `source .venv/bin/activate` (macOS/Linux)

#### 실행할 명령어

**가상환경 활성화 확인:**
```bash
# 프롬프트에 (.venv)가 표시되어야 함
(.venv) PS D:\...\workout-counter-pc>
```

**기본 테스트 (MediaPipe Pose 확인):**
```bash
python test_pose_webcam.py
```

**또는 v0.4 버전이 필요한 경우:**

> **⚠️ 참고**: 현재 저장소의 main 브랜치에는 `test_pose_webcam.py`와 `test_camera.py`만 있습니다.  
> v0.4 버전의 `main_squat.py` 등 운동별 카운팅 파일이 필요하다면:
> 
> ```bash
> # v0.4 브랜치로 전환
> git checkout v0.4
> 
> # 또는 v0.4 태그로 전환
> git checkout tags/v0.4
> 
> # v0.4 폴더로 이동 후 실행
> cd v0.4
> python main_squat.py
> ```

#### 예상 결과

**test_pose_webcam.py 실행 시:**
1. 웹캠 창이 열림
2. 스켈레톤 오버레이가 표시됨
3. ESC 키를 누르면 종료됨

**main_squat.py 실행 시 (v0.4 버전):**
1. 웹캠 창이 열림
2. 스켈레톤 오버레이가 표시됨
3. 카운트 정보가 화면에 표시됨
4. READY → CALI → COUNT 상태 전환

#### 확인 사항

**test_pose_webcam.py:**
- [ ] 웹캠 창이 열리는가?
- [ ] 스켈레톤이 표시되는가?
- [ ] 사람이 움직이면 스켈레톤이 따라오는가?

**main_squat.py (v0.4):**
- [ ] 웹캠 창이 열리는가?
- [ ] 스켈레톤이 표시되는가?
- [ ] 실제로 스쿼트를 하면 카운트가 올라가는가?
- [ ] READY → CALI → COUNT 상태 전환이 정상인가?

#### 종료 방법

- **test_pose_webcam.py**: ESC 키를 누르면 종료됩니다
- **main_squat.py**: `q` 키를 누르면 종료됩니다

#### 문제 발생 시

**문제**: 웹캠이 인식되지 않음
- **해결**: 
  - 다른 프로그램에서 웹캠 사용 중인지 확인
  - 코드에서 카메라 번호 확인: `cv2.VideoCapture(0)` → `cv2.VideoCapture(1)` 시도
  - macOS: 카메라 권한 확인 필요 (시스템 설정 → 보안 및 개인정보 보호 → 카메라)
  - macOS: 터미널 앱에 카메라 권한 부여 필요할 수 있음

**문제**: `ModuleNotFoundError: No module named 'mediapipe'`
- **원인**: 가상환경이 활성화되지 않았거나, 라이브러리가 설치되지 않음
- **해결**: 
  1. 가상환경 활성화 확인
     ```bash
     # Windows PowerShell
     .\.venv\Scripts\activate
     
     # Windows CMD
     .venv\Scripts\activate.bat
     
     # macOS/Linux
     source .venv/bin/activate
     ```
  2. 프롬프트에 `(.venv)`가 표시되는지 확인
  3. 라이브러리 설치 확인: `pip list | findstr mediapipe` (Windows) 또는 `pip list | grep mediapipe` (macOS/Linux)
  4. 설치되지 않았다면: `pip install mediapipe opencv-python numpy`

**문제**: `AttributeError: module 'mediapipe' has no attribute 'solutions'`
- **원인**: MediaPipe 최신 버전(0.10.30+)에서는 `solutions` 모듈이 제거되었습니다. 프로젝트는 MediaPipe 0.10.21을 기준으로 작성되었습니다.
- **해결**: 
  1. **Python 3.10.8 사용 권장** (가장 확실한 방법)
     - Python 3.10.8 설치: https://www.python.org/downloads/release/python-3108/
     - 새 가상환경 생성: `python3.10 -m venv .venv`
     - MediaPipe 0.10.21 설치: `pip install mediapipe==0.10.21`
  
  2. **MediaPipe 0.10.21로 다운그레이드 시도** (Python 3.13에서는 작동하지 않을 수 있음)
     ```bash
     pip uninstall mediapipe -y
     pip install mediapipe==0.10.21
     ```
     - Python 3.13에서는 MediaPipe 0.10.21이 지원되지 않을 수 있음
     - 이 경우 Python 3.10.8 사용 필수
  
  3. **확인 방법**:
     ```bash
     python -c "import mediapipe as mp; print(hasattr(mp, 'solutions'))"
     ```
     - `True`가 나와야 정상
     - `False`가 나오면 MediaPipe 버전 문제

**문제**: 포즈 인식이 안 됨
- **해결**: 
  - 조명 환경 개선
  - 카메라 위치 조정 (정면, 전신이 보이도록)
  - 전신이 화면에 들어오도록 조정

---

## 5. 운영 환경 설정

### 5.1 권장 촬영 환경

#### 카메라 위치

| 운동 | 권장 높이 | 비고 |
|------|----------|------|
| **스쿼트** | 90cm (배~가슴 높이) | CALIB 빠름, 포즈 인식 안정 |
| **푸쉬업** | 65cm (무릎~골반 높이) | 카운트 안정적 |
| **크런치** | 65cm (무릎~골반 높이) | 포즈 유지 필요 |
| **버피테스트** | 65cm (무릎~골반 높이) | Light/Dark 모두 안정적 |

**⚠️ 중요:**
- ❌ **바닥 위치는 부적합**: 포즈 인식 실패, CALIB 어려움
- ✅ **삼각대 고정 필수**: 손으로 들고 있으면 미세한 떨림으로 READY 실패

#### 조명 환경

- ✅ **밝은 환경 권장**: Light 환경이 Dark 환경보다 항상 안정적
- ⚠️ **Dark 환경**: 포즈 인식률 저하, 카운트 정확도 감소

#### 촬영 각도

- **스쿼트/푸쉬업**: 정면 촬영
- **크런치**: 정면보다 약간 위쪽 (45~60도) 권장

---

## 6. 트러블슈팅

> **💡 빠른 참조**: 자주 발생하는 문제와 해결 방법을 정리했습니다.  
> 각 섹션의 "문제 발생 시" 부분도 참고하세요.

### 6.1 자주 발생하는 문제 요약

#### Python 관련 문제

| 문제 | 원인 | 해결 방법 | 참고 섹션 |
|------|------|----------|----------|
| `ModuleNotFoundError: No module named 'mediapipe'` | 가상환경 미활성화 | `deactivate` 후 `.\.venv\Scripts\activate` | [PC 버전 5단계](#5단계-실행-및-확인-1) |
| `AttributeError: module 'mediapipe' has no attribute 'solutions'` | MediaPipe 버전 문제 (0.10.30+) | Python 3.10.8 사용 + MediaPipe 0.10.21 설치 | [Python 3.10.8 설치 가이드](#-python-3108-설치-가이드) |
| `ERROR: Could not find a version that satisfies the requirement mediapipe==0.10.21` | Python 3.13+ 환경 | Python 3.10.8 설치 필수 | [Python 3.10.8 설치 가이드](./Python_3.10.8_설치_가이드.md) |
| Python 버전이 3.13+ | 최신 Python 사용 중 | Python 3.10.8 다운그레이드 | [Python 3.10.8 설치 가이드](./Python_3.10.8_설치_가이드.md) |

#### 가상환경 관련 문제

| 문제 | 원인 | 해결 방법 | 참고 섹션 |
|------|------|----------|----------|
| 가상환경 비활성화 방법 모름 | 명령어 미숙지 | `deactivate` 명령어 사용 | [PC 버전 3단계](#3단계-가상환경-생성) |
| 가상환경 재생성 필요 | Python 버전 변경 | `deactivate` → 폴더 삭제 → 재생성 | [Python 3.10.8 설치 가이드](./Python_3.10.8_설치_가이드.md) |
| 가상환경 활성화 실패 (Windows) - 보안 오류 | PowerShell 실행 정책 | `Set-ExecutionPolicy RemoteSigned -Scope CurrentUser` 또는 CMD에서 `activate.bat` 사용 | [PC 버전 3단계](#3단계-가상환경-생성) |

#### 파일 구조 관련 문제

| 문제 | 원인 | 해결 방법 | 참고 섹션 |
|------|------|----------|----------|
| `v0.4` 폴더가 없음 | main 브랜치에는 테스트 파일만 있음 | `git checkout v0.4` 실행 | [PC 버전 5단계](#5단계-실행-및-확인-1) |
| `main_squat.py` 파일이 없음 | v0.4 브랜치에 있음 | `git checkout v0.4` 후 `cd v0.4` | [PC 버전 5단계](#5단계-실행-및-확인-1) |

#### 실행 관련 문제

| 문제 | 원인 | 해결 방법 | 참고 섹션 |
|------|------|----------|----------|
| 웹캠이 인식되지 않음 | 다른 프로그램 사용 중 또는 권한 문제 | 프로그램 종료, 카메라 번호 변경, macOS 권한 확인 | [PC 버전 5단계](#5단계-실행-및-확인-1) |
| 포즈 인식이 안 됨 | 조명/위치 문제 | 조명 개선, 카메라 위치 조정 | [PC 버전 5단계](#5단계-실행-및-확인-1) |

#### macOS/Linux 관련 문제

| 문제 | 원인 | 해결 방법 | 참고 섹션 |
|------|------|----------|----------|
| 카메라 권한 오류 (macOS) | 터미널 앱 권한 없음 | 시스템 설정 → 보안 → 카메라 → 터미널 체크 | [macOS 호환성](#macoslinux-호환성-참고) |
| `python` 명령어가 작동 안 함 (macOS/Linux) | `python3` 사용 필요 | `python3` 명령어 사용 | [macOS 호환성](#macoslinux-호환성-참고) |
| 가상환경 활성화 명령어 다름 | OS별 차이 | `source .venv/bin/activate` 사용 | [macOS 호환성](#macoslinux-호환성-참고) |

---

### 6.2 단계별 문제 해결 가이드

#### Windows PowerShell 보안 오류 상세 해결

**문제**: `.\.venv\Scripts\activate` 실행 시 보안 오류 발생

**단계별 해결:**

1. **현재 실행 정책 확인**
   ```powershell
   Get-ExecutionPolicy
   ```
   - `Restricted`: 스크립트 실행 불가 (기본값)
   - `RemoteSigned`: 로컬 스크립트 실행 가능 (권장)
   - `Unrestricted`: 모든 스크립트 실행 가능 (비권장)

2. **실행 정책 변경 (현재 사용자만)**
   ```powershell
   Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```
   - 확인 메시지: `Y` 입력
   - 다른 사용자에게 영향 없음
   - 관리자 권한 불필요

3. **가상환경 활성화 재시도**
   ```powershell
   .\.venv\Scripts\activate
   ```

4. **여전히 안 되면 CMD 사용**
   ```cmd
   # PowerShell 종료 후 CMD 실행
   .venv\Scripts\activate.bat
   ```

**대안: CMD 사용하기**
- PowerShell 대신 CMD(명령 프롬프트) 사용
- CMD는 실행 정책 제한이 없음
- 명령어만 약간 다름: `.venv\Scripts\activate.bat`

### 모바일 버전

#### 문제: READY 단계에서 진행되지 않음

**증상**: 앱 실행 후 카운팅 화면에서 "READY..."만 표시되고 진행되지 않음

**원인:**
- 카메라 흔들림 (삼각대 미사용)
- 포즈 인식 불안정
- 조명 부족

**해결:**
1. ✅ **삼각대 필수 사용**: 손으로 들고 있으면 안 됨
2. 조명 환경 개선
3. 전신이 화면에 잘 보이도록 위치 조정

#### 문제: 카운트가 정확하지 않음

**증상**: 실제 운동 횟수와 카운트가 다름

**원인:**
- 임계값이 사용자/환경에 맞지 않음
- CALI 단계에서 잘못된 기준값 저장

**해결:**
1. CALI 단계에서 올바른 자세 유지
2. 카운트 초기화 버튼으로 재시작
3. 촬영 환경을 권장 환경으로 조정

### PC 버전

#### 문제: 웹캠이 인식되지 않음

**증상**: `cv2.VideoCapture()` 실행 시 오류

**원인:**
- 다른 프로그램에서 웹캠 사용 중
- 카메라 번호 불일치

**해결:**
1. 다른 프로그램 종료
2. 코드에서 카메라 번호 변경: `cv2.VideoCapture(0)` → `cv2.VideoCapture(1)`

---

## HEADPOSE 설치 및 실행

> **⚠️ 참고**: HEADPOSE는 프로젝트3와는 별개의 실험 프로젝트입니다.  
> MediaPipe FaceMesh를 사용한 얼굴 방향/감정 추정 실험입니다.

### 📌 전체 설치 체크리스트

- [ ] 1단계: Python 설치 확인 (workout-counter-pc와 동일)
- [ ] 2단계: 저장소 클론
- [ ] 3단계: 가상환경 생성 (workout-counter-pc와 동일)
- [ ] 4단계: 라이브러리 설치
- [ ] 5단계: 실행 및 확인

---

### 1단계: Python 설치 확인

**목적**: Python이 제대로 설치되어 있는지 확인합니다.

> **💡 팁**: workout-counter-pc를 이미 설치했다면 이 단계는 건너뛰어도 됩니다.

#### 실행할 명령어

```bash
python --version
```

#### 예상 결과

```
Python 3.10.8
```

#### 확인 사항

- [ ] Python 버전이 3.10 이상인가?
- [ ] `python` 명령어가 인식되는가?

#### 문제 발생 시

**문제**: `'python'은(는) 내부 또는 외부 명령...` (Windows) 또는 `command not found: python` (macOS/Linux)
- **해결**: [PC 버전 설치 및 실행](#1단계-python-설치-확인) 섹션의 "문제 발생 시" 부분을 참고하세요

#### 다음 단계

Python이 정상 설치되면 → [2단계: 저장소 클론](#2단계-저장소-클론-2)으로 이동

---

### 2단계: 저장소 클론

**목적**: GitHub에서 HEADPOSE 프로젝트 코드를 가져옵니다.

#### 실행할 명령어

**터미널 위치**: 원하는 작업 폴더 (workout-counter-pc와 같은 폴더에 두면 편리합니다)

```bash
git clone https://github.com/pnkmem432/HEADPOSE.git
cd HEADPOSE
```

#### 예상 결과

```
Cloning into 'HEADPOSE'...
...
Resolving deltas: 100% (XXX/XXX), done.
```

#### 확인 사항

- [ ] `HEADPOSE` 폴더가 생성되었는가?
- [ ] `head_pose.py` 파일이 있는가?
- [ ] `headpose_webcam_direction.py` 파일이 있는가?

#### 다음 단계

저장소 클론이 완료되면 → [3단계: 가상환경 생성](#3단계-가상환경-생성-1)으로 이동

---

### 3단계: 가상환경 생성

**목적**: 프로젝트 전용 Python 환경을 만듭니다.

> **💡 팁**: workout-counter-pc를 이미 설치했다면, 가상환경 생성 방법은 동일합니다.

#### 실행할 명령어

```bash
python -m venv .venv
```

#### 예상 결과

명령어 실행 후 아무 메시지도 나오지 않으면 성공입니다.

#### 확인 사항

- [ ] `.venv` 폴더가 생성되었는가?
- [ ] `.venv/Scripts/` 폴더가 있는가? (Windows) 또는 `.venv/bin/` 폴더가 있는가? (macOS/Linux)

#### 가상환경 활성화

**Windows PowerShell:**
```bash
.\.venv\Scripts\activate
```

**Windows CMD:**
```bash
.venv\Scripts\activate.bat
```

**macOS / Linux:**
```bash
source .venv/bin/activate
```

**활성화 성공 시:**
- Windows: `(.venv) PS C:\...\HEADPOSE>`
- macOS/Linux: `(.venv) user@macbook HEADPOSE %`

프롬프트 앞에 `(.venv)`가 표시되면 성공입니다.

#### 다음 단계

가상환경이 활성화되면 → [4단계: 라이브러리 설치](#4단계-라이브러리-설치-1)로 이동

---

### 4단계: 라이브러리 설치

**목적**: HEADPOSE 프로젝트가 사용하는 Python 라이브러리들을 설치합니다.

> **💡 팁**: HEADPOSE는 여러 파일이 있어서 필요한 라이브러리가 다를 수 있습니다.  
> 기본적인 얼굴 방향 추정을 위해서는 아래 라이브러리만 설치하면 됩니다.

#### 기본 라이브러리 설치 (얼굴 방향 추정용)

**가상환경이 활성화된 상태에서:**
```bash
pip install mediapipe==0.10.21
pip install opencv-python==4.11.0.86
pip install numpy==1.26.4
```

#### 예상 결과

```
Collecting mediapipe==0.10.21
  Downloading mediapipe-0.10.21-cp310-cp310-win_amd64.whl (XX.X MB)
...
Successfully installed mediapipe-0.10.21 opencv-python-4.11.0.86 numpy-1.26.4
```

#### 추가 라이브러리 (고급 기능용)

**감정 추정을 위해서는:**
```bash
pip install pillow==10.2.0
```

**Zone 인식/YOLO Pose를 위해서는:**
```bash
pip install ultralytics
```

#### 확인 사항

- [ ] `Successfully installed` 메시지가 나오는가?
- [ ] 설치된 패키지 확인: `pip list`
  - `mediapipe 0.10.21` 표시되는가?
  - `opencv-python 4.11.0.86` 표시되는가?
  - `numpy 1.26.4` 표시되는가?

#### 문제 발생 시

**문제**: `pip` 명령어가 인식되지 않음
- **해결**: 가상환경이 활성화되었는지 확인
  - 프롬프트에 `(.venv)` 표시 확인

**문제**: 패키지 다운로드 실패
- **해결**: 
  - 인터넷 연결 확인
  - 다시 시도: `pip install mediapipe==0.10.21`

#### 다음 단계

라이브러리 설치가 완료되면 → [5단계: 실행 및 확인](#5단계-실행-및-확인-2)로 이동

---

### 5단계: 실행 및 확인

**목적**: HEADPOSE가 정상 동작하는지 확인합니다.

#### 기본 실행: 얼굴 방향 추정

**실행할 명령어:**
```bash
python headpose_webcam_direction.py
```

**또는 기본 버전:**
```bash
python head_pose.py
```

#### 예상 결과

1. 웹캠 창이 열림
2. 얼굴이 인식되면 얼굴 박스가 표시됨
3. yaw/pitch/roll 각도 값이 화면에 표시됨

**화면 예시:**
- 얼굴 박스 (초록색 사각형)
- "yaw: -5.2 pitch: 10.1 roll: 2.3" 같은 텍스트
- 얼굴 랜드마크 점들 (선택적)

#### 확인 사항

- [ ] 웹캠 창이 열리는가?
- [ ] 얼굴이 인식되는가? (얼굴 박스가 표시되는가?)
- [ ] yaw/pitch/roll 값이 표시되는가?
- [ ] 고개를 좌우로 돌리면 yaw 값이 변하는가?

#### 종료 방법

- ESC 키를 누르면 종료됩니다

#### 문제 발생 시

**문제**: 웹캠이 인식되지 않음
- **해결**: 
  - 다른 프로그램에서 웹캠 사용 중인지 확인
  - 코드에서 카메라 번호 확인: `cv2.VideoCapture(0)` → `cv2.VideoCapture(1)` 시도
  - macOS: 카메라 권한 확인 (시스템 설정 → 보안 및 개인정보 보호 → 카메라)

**문제**: 얼굴이 인식되지 않음
- **해결**: 
  - 조명이 충분한지 확인
  - 얼굴이 화면 중앙에 있는지 확인
  - 카메라에서 얼마나 떨어져 있는지 확인 (너무 가깝거나 멀면 안 됨)

**문제**: `ModuleNotFoundError: No module named 'mediapipe'`
- **해결**: 가상환경이 활성화되었는지 확인
  - 프롬프트에 `(.venv)` 표시 확인
  - `pip list`로 mediapipe가 설치되어 있는지 확인

---

### 추가 기능 실행

HEADPOSE에는 여러 기능이 있습니다. 기본 얼굴 방향 추정이 정상 동작하면 아래 기능들도 시도해볼 수 있습니다.

#### 감정 추정 (단일 인원)

**필수 라이브러리:**
```bash
pip install pillow==10.2.0
```

**실행:**
```bash
python emotion_mediapipe.py
```

**기능:**
- 얼굴 랜드마크로 표정 분석
- neutral/happy/sad/angry/surprised 감정 분류
- 이모지로 감정 표시

#### 감정 추정 (다인원)

**실행:**
```bash
python emotion_mediapipe_multi.py
```

**기능:**
- 최대 5명의 얼굴 동시 인식
- 각 사람별 감정 분류
- ID + 감정 + 이모지 표시

#### Zone 인식 (YOLO Pose + FaceMesh)

**필수 라이브러리:**
```bash
pip install ultralytics
```

**실행:**
```bash
python face_body_pose.py
```

**기능:**
- 화면을 4분할 (Zone 1~4)
- YOLO Pose로 사람 검출 및 스켈레톤 추정
- 발 위치로 Zone 판정
- Zone별 인원 수 카운트

#### 매트 ROI + Zone 인식

**실행:**
```bash
python zone.py
```

**기능:**
- 매트 영역을 4점으로 지정하여 보정
- 보정된 매트 이미지에서 Zone 기준선 자동 검출
- 발 위치를 매트 좌표로 변환하여 Zone 판정

---

## macOS/Linux 호환성 참고

> **⚠️ 중요**: 본 가이드는 Windows 기준으로 작성되었지만, macOS/Linux에서도 대부분 동일하게 작동합니다.  
> 아래 차이점만 확인하면 macOS/Linux에서도 문제없이 설치 및 실행 가능합니다.

### 주요 차이점

#### 1. 가상환경 활성화

**Windows:**
```bash
.\.venv\Scripts\activate
```

**macOS/Linux:**
```bash
source .venv/bin/activate
```

#### 2. Python 명령어

**Windows:**
```bash
python --version
python -m venv .venv
```

**macOS/Linux:**
```bash
python3 --version
python3 -m venv .venv
```

#### 3. Flutter Doctor 차이

**Windows:**
- `[✓] Visual Studio - develop Windows apps`

**macOS:**
- `[✓] Xcode - develop for iOS and macOS`

**Linux:**
- Visual Studio 또는 Xcode 항목 없음 (정상)

#### 4. Gradle 실행

**Windows:**
```bash
cd android
gradlew.bat clean
```

**macOS/Linux:**
```bash
cd android
./gradlew clean
```

#### 5. 카메라 권한 (macOS)

macOS에서는 터미널 앱에 카메라 권한을 부여해야 할 수 있습니다:

1. **시스템 설정** → **보안 및 개인정보 보호** → **카메라**
2. 터미널 앱 체크
3. 또는 코드 실행 시 권한 요청 팝업에서 허용

**권한 확인 방법:**
```bash
# 터미널에서 카메라 접근 테스트
python3 -c "import cv2; cap = cv2.VideoCapture(0); print('카메라 접근 가능' if cap.isOpened() else '카메라 접근 불가')"
```

#### 6. 경로 예시

**Windows:**
- `C:\WS\flutter_projects`
- `D:\projects`

**macOS/Linux:**
- `~/projects`
- `/Users/username/projects` (macOS)
- `/home/username/projects` (Linux)

### 호환성 확인

#### PC 버전
- ✅ Python 3.10.8: macOS/Linux 지원
- ✅ MediaPipe 0.10.21: macOS/Linux 지원
- ✅ OpenCV 4.11.0.86: macOS/Linux 지원
- ⚠️ 웹캠: macOS에서 카메라 권한 필요

#### 모바일 버전
- ✅ Flutter: 크로스 플랫폼 (Windows/macOS/Linux 모두 지원)
- ✅ Android 개발: macOS에서도 가능
- ✅ 모든 명령어: 크로스 플랫폼 호환

### macOS 특이사항

1. **카메라 권한**: 터미널 앱에 카메라 권한 부여 필요
2. **Python 명령어**: `python3` 사용 권장
3. **가상환경**: `source .venv/bin/activate` 사용
4. **경로 구분자**: `/` 사용 (Windows는 `\`)

### Linux 특이사항

1. **카메라 권한**: 일반적으로 권한 문제 없음
2. **Python 명령어**: `python3` 사용
3. **가상환경**: `source .venv/bin/activate` 사용
4. **웹캠**: V4L2 (Video4Linux2) 사용

### 추가 참고

더 자세한 macOS 호환성 정보는 `.cursor/mac-compatibility.md` 파일을 참고하세요.

---

## 7. 참고 자료

### 저장소

- **PC 버전**: https://github.com/pnkmem432/workout-counter-pc
- **모바일 버전**: https://github.com/pnkmem432/workout-counter-mobile
- **HEADPOSE**: https://github.com/pnkmem432/HEADPOSE
- **구글 드라이브**: https://drive.google.com/drive/folders/1r3qVubDVXiEYmKXj-QL-3Q1kLxstGouo?hl=ko

### 기술 문서

- **MediaPipe 공식 문서**: https://mediapipe.readthedocs.io/
- **Flutter 공식 문서**: https://flutter.dev/docs

### 관련 가이드 문서

- **Python 3.10.8 설치 가이드**: [`Python_3.10.8_설치_가이드.md`](./Python_3.10.8_설치_가이드.md)
- **macOS 호환성 가이드**: [`.cursor/mac-compatibility.md`](./.cursor/mac-compatibility.md)
- **프로젝트 점검 체크리스트**: [`프로젝트3_점검_체크리스트.md`](./프로젝트3_점검_체크리스트.md)
- **프로젝트 개요**: [`project3info.md`](./project3info.md)

### 주요 학습 내용 요약

**지금까지 대화에서 확인된 주요 사항:**

1. **Python 버전 중요성**
   - Python 3.10.8 필수 (MediaPipe 0.10.21 호환)
   - Python 3.13+에서는 MediaPipe 0.10.21 설치 불가
   - 해결: Python 3.10.8 설치 후 가상환경 재생성

2. **가상환경 관리**
   - 활성화: `.\.venv\Scripts\activate` (Windows) 또는 `source .venv/bin/activate` (macOS/Linux)
   - 비활성화: `deactivate`
   - 재생성: `deactivate` → 폴더 삭제 → `py -3.10 -m venv .venv`

3. **파일 구조 이해**
   - main 브랜치: `test_pose_webcam.py`, `test_camera.py`만 있음
   - v0.4 버전: `git checkout v0.4` 후 `cd v0.4`에서 `main_squat.py` 등 실행

4. **HEADPOSE와 workout-counter-pc의 관계**
   - 별도 프로젝트이지만 기술 스택 동일
   - 설치 순서: workout-counter-pc → HEADPOSE (권장)
   - 개발 환경 설정 거의 동일

5. **macOS 호환성**
   - 카메라 권한 필요 (시스템 설정에서 터미널 앱 권한 부여)
   - `python3` 명령어 사용
   - `source .venv/bin/activate` 사용

---

**작성일**: 2026-01-27  
**작성자**: 인수인계 가이드 작성 (AI Co-Lead)  
**최종 업데이트**: 2026-01-27 (Python 3.10.8 설치 가이드 추가, 트러블슈팅 섹션 보강)
